# User Management CRUD Dashboard

A full-stack admin dashboard for managing user accounts — built with **React (Vite)** on the
frontend and **Python / FastAPI** on the backend, persisted to **SQLite**.

> **A note on "AgentRise™ Harness":** this project was requested for use with an "AgentRise
> Harness" CLI. That tool isn't something I have reliable knowledge of, and I didn't have
> network access while building this to look it up or install it — so I have **not** fabricated
> `harness` commands or config anywhere in this repo. See [Harness Workflow](#harness-workflow)
> below for how to wire it in once you confirm the real command set.

---

## 1. Project Overview

Administrators can create, view, update, delete, search, and filter user records from a
browser-based dashboard. All data is persisted in SQLite via SQLAlchemy — there is no
hardcoded or in-memory user data.

## 2. Architecture

```text
Browser (React UI)
      │  Axios (JSON over HTTP)
      ▼
FastAPI app  (app/main.py)
      │
      ▼
API routers   (app/api/*.py)          — parses requests, returns responses
      │
      ▼
Service layer (app/services/*.py)     — business rules (uniqueness, 404s)
      │
      ▼
Repository    (app/repositories/*.py) — all SQLAlchemy query logic
      │
      ▼
SQLAlchemy ORM → SQLite (users.db)
```

The frontend mirrors this separation: components never call Axios directly — every HTTP call
goes through `frontend/src/services/userApi.js`.

## 3. Technologies

**Frontend:** React 18, Vite, Axios, plain modern CSS (custom design system, no UI framework),
Vitest for tests.

**Backend:** Python 3.11+, FastAPI, Pydantic v2, SQLAlchemy 2.0, SQLite, Uvicorn, pytest +
httpx (via FastAPI's `TestClient`).

## 4. Project Structure

```text
react-python-crud/
├── backend/
│   ├── app/
│   │   ├── api/            # HTTP routers (users, dashboard)
│   │   ├── models/         # SQLAlchemy models
│   │   ├── schemas/        # Pydantic request/response schemas + validation
│   │   ├── repositories/   # Data-access layer (SQLAlchemy queries)
│   │   ├── services/       # Business logic
│   │   ├── database.py     # Engine / session / Base
│   │   ├── seed.py         # First-run sample data
│   │   └── main.py         # App wiring: CORS, routers, error handlers
│   ├── tests/
│   │   └── test_users.py
│   ├── requirements.txt
│   └── .env.example
├── frontend/
│   ├── src/
│   │   ├── components/     # Sidebar, Header, StatsCard, UserTable, UserForm, ...
│   │   ├── pages/
│   │   │   ├── Dashboard.jsx
│   │   │   └── Users.jsx
│   │   ├── services/
│   │   │   └── userApi.js  # The ONLY module that calls the backend
│   │   ├── styles/index.css
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── package.json
│   ├── vite.config.js
│   └── .env.example
└── README.md
```

## 5. Prerequisites

- Python 3.11+
- Node.js 18+ and npm
- No external database needed — SQLite is a local file created automatically.

## 6. Backend Installation

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env             # adjust if needed
```

## 7. Frontend Installation

```bash
cd frontend
npm install
cp .env.example .env             # adjust VITE_API_URL if needed
```

## 8. Running the Backend

```bash
cd backend
source .venv/bin/activate
uvicorn app.main:app --reload
```

- API base URL: `http://localhost:8000`
- Interactive API docs (Swagger UI): `http://localhost:8000/docs`
- On first run, the `users` table is created and 5 sample users are seeded automatically.
  Restarting the server will **not** duplicate them.

## 9. Running the Frontend

```bash
cd frontend
npm run dev
```

- App URL: `http://localhost:5173`

Open `http://localhost:5173` in your browser — the dashboard loads live data from the FastAPI
backend on `http://localhost:8000`.

## 10. API Documentation

Base path: `/api`

| Method | Endpoint                | Description                                   |
|--------|--------------------------|------------------------------------------------|
| GET    | `/api/users`             | List users. Query params: `search`, `role`, `status` |
| GET    | `/api/users/{id}`        | Get one user                                   |
| POST   | `/api/users`              | Create a user                                  |
| PUT    | `/api/users/{id}`        | Update a user                                  |
| DELETE | `/api/users/{id}`        | Delete a user (returns `204 No Content`)       |
| GET    | `/api/dashboard/stats`   | `{ total_users, active_users, admin_users, regular_users }` |
| GET    | `/api/health`            | Basic health check                             |
| POST   | `/api/auth/login`        | Log in with email + password; returns `{ token }` (signed JWT) |

Full interactive documentation (generated by FastAPI) is at `/docs` while the backend is
running.

**Request body for POST/PUT `/api/users`:**

```json
{
  "name": "John Smith",
  "email": "john@test.com",
  "phone": "9876543210",
  "role": "User",
  "status": "Active",
  "password": "correct-horse-battery-staple"
}
```

`password` is **required on `POST /api/users`** (min 8 characters) — the server hashes it with
bcrypt (work factor 12) before persisting; the plaintext value is never stored, logged, or
returned. No API response — list, get, create, or update — ever includes a `password` or
`password_hash` field.

**Validation rules:** `name` ≥ 2 chars, `email` must be a valid address, `phone` must match
`^[0-9+\-() ]{7,20}$`, `role` ∈ `{Admin, User}`, `status` ∈ `{Active, Inactive}`, `password` ≥ 8
chars (required on create), `email` must be unique (a duplicate returns `409 Conflict`).

**Request body for POST `/api/auth/login`:**

```json
{
  "email": "john@test.com",
  "password": "correct-horse-battery-staple"
}
```

On success, returns `200` with `{ "token": "<jwt>" }`. On an unknown email, a wrong password,
or an account that has not yet been provisioned with a real password (see §11a below), returns
`401` with body `{"detail": "Invalid email or password."}` — the response is identical in all
three cases, so it never reveals whether an account exists. After 3 failed attempts for the
same email within a rolling 60-minute window, further attempts return `429` with a
`Retry-After` header.

### 11a. Provisioning an initial password for an existing user

Every user row created before this feature has no credential and cannot log in until an
operator sets one (the CRUD API now requires a `password` on every new user — see §10). On
startup, the
backend automatically assigns every credential-less row a random, unusable password hash and
marks it `must_reset_password = true` — this only prevents an unauthenticated login, it does
not give the user a usable password.

To give a specific user a real initial password, an operator with direct access to the backend
server runs:

```bash
cd backend
source .venv/bin/activate
python -m app.scripts.set_initial_password <email>
```

The script prompts for the new password interactively (input is hidden, never taken as a CLI
argument or environment variable, and never logged or echoed back). On success it prints
`Password set for user <email>` and exits `0`; on failure (e.g. no such user) it prints a
generic error to stderr and exits `1`. This script is **not** reachable via any HTTP endpoint —
it requires direct server/deployment access.

## 11. CRUD Usage (from the UI)

CRUD, search, filtering, and pagination all live on the dedicated **Users** page (sidebar →
"Users"), not on the Dashboard. Dashboard is overview-only — it shows the Total/Active/Admins/
Regular Users stats cards and no longer fetches the user list.

1. **Create** — click **+ Add User**, fill the form, submit. Validation runs client-side first;
   the server re-validates and rejects duplicate emails.
2. **Read** — the table loads from `GET /api/users` on page load and after every filter/search
   change. Click a row's view icon for the full detail view (`GET /api/users/{id}`).
3. **Update** — click a row's edit icon, change fields, submit (`PUT /api/users/{id}`).
4. **Delete** — click a row's delete icon → confirm in the dialog → `DELETE /api/users/{id}`.
5. **Search** — the search box filters by name, email, or phone (debounced, backend-driven).
6. **Filter** — the Role and Status dropdowns filter server-side alongside search.
7. **Logout** — from the Settings screen, click **Logout** to clear the client-side session and
   redirect to the Login screen; this makes no server-side call.

The **Settings** screen (sidebar → "Settings") is read-only: it shows the signed-in user's own
name and email (`GET /api/auth/me`) plus the Logout action above — there is no account-editing
or password-change UI on this screen.

Every mutation shows a loading state on its button, a success toast on completion, and a
readable error message (never a raw stack trace) on failure.

## 12. Testing

**Backend:**

```bash
cd backend
source .venv/bin/activate
pytest
```

Covers: create, list, get-by-id, update, delete, 404s, duplicate email (409), and validation
errors (422) for bad email/phone/missing fields, plus dashboard stats and search/filter logic.

**Frontend:**

```bash
cd frontend
npm run test
```

## 13. Harness Workflow

I could not verify an installed `harness` CLI or its command set in the environment this
project was built in (no `harness` binary present, and no network access to check). Rather
than invent commands, here's what to do:

1. Run `harness --version` and `harness --help` locally and share the output.
2. I'll map the real subcommands (`init`, `generate`, `validate`, `analyse`, `audit`, `explain`,
   `list`, etc.) onto this repo's actual structure — the layered backend (`api → service →
   repository → models`) and the componentized frontend are already organized in a way that
   should make requirement-tracing, validation, and auditing straightforward for a tool like
   this.
3. If Harness expects a manifest file (e.g. `harness.yaml`/`harness.json`) at the project root,
   send me its schema (or run `harness init` interactively) and I'll generate one that reflects
   this project's real endpoints, models, and components — not placeholder content.

## 14. Troubleshooting

| Symptom | Likely cause / fix |
|---|---|
| Frontend shows "Could not reach the server" | Backend isn't running, or is on a different port than `VITE_API_URL` in `frontend/.env` |
| CORS error in browser console | Backend `CORS_ORIGINS` (in `backend/.env`) doesn't include your frontend's URL |
| `409 Conflict` on create/update | Email already exists on another user |
| `422` on create/update | Check the response body's `detail` — it names the invalid field |
| Table shows old data after edit | Shouldn't happen — the app refetches after every mutation. Hard-refresh if it persists |
| `users.db` has stale/no data | Delete `backend/users.db` and restart the backend to reseed |
| Existing/seeded user cannot log in | The account has no real password yet — run `python -m app.scripts.set_initial_password <email>` (see §11a) to set one |
| `pip install` fails | Make sure you're using Python 3.11+ and have activated the virtualenv |
| `npm install` fails | Make sure you're using Node.js 18+ |

---

Built as a reference implementation of a clean, layered CRUD application — feel free to extend
the `role`/`status` enums, add authentication, or swap SQLite for Postgres by changing only
`DATABASE_URL`.
