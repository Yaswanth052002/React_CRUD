# Validation report — AUTH-07 (round 3)

- Date: 2026-08-18T13:00:00Z
- Verdict: PARTIAL
- Mode: rerun-failed (only AUTH-07-TC-11 re-evaluated; TC-01..TC-10 keep their round-2 PASS results from `docs/features/AUTH-07/VALIDATION-20260818-1245.md`, independently re-confirmed by a fresh full-suite run this pass)
- Total: 11   Passed: 10   Failed: 1   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: included in the same session as AUTH-06's validation pass (~620s combined)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Integration  |   6   |   6  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| E2E (declared, run under Vitest) |   3   |   2  |   1  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

E2E row: TC-01/TC-04 PASS, TC-11 FAIL (see below). Stack smoke reuses the same two stacks
validated in this session's AUTH-06 pass (react, fastapi) — both PASS.

### Task completion verification (MANDATORY)

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/src/services/userApi.js` (add `logout()` export) | ✓ | ✓ (`export function logout()` at line 66) | PASS |
| T-02 | `frontend/src/services/__tests__/userApi.logout.test.js` (functional coverage) | ✓ | n/a (feature/test work, file non-empty) | PASS |
| T-03 | `frontend/src/services/__tests__/userApi.logout.test.js` (in-flight-request race, TC-09) | ✓ | ✓ (`describe`/`it` block for TC-09 present and passing) | PASS |
| T-04 | `frontend/src/services/__tests__/userApi.logout.test.js` (p95 latency benchmark, TC-10) | ✓ | ✓ (TC-10 block present, passing) | PASS |
| T-05 | `README.md` (§ 11 Logout behavior note) | ✓ | ✓ (`README.md:230-231` — "**Logout** — from the Settings screen, click **Logout** to clear the client-side session and ...") | PASS |

All 5 PLAN tasks verified PASS. No task-completion failures.

### Round 3 focus: AUTH-07-TC-11 re-validation

**Prior state (round 2, `VALIDATION-20260818-1245.md`):** FAIL/ERROR — "No flow/test exists.
PLAN.md attributes coverage to AUTH-06's Settings.test.jsx, but AUTH-06 is unimplemented on this
branch ... orphan TC with no runnable artefact." Tracked as `known_gap` AF-04, kind `sequencing`.

**What changed since round 2:** AUTH-06 has now been implemented on this same working tree —
`frontend/src/pages/Settings.jsx` and `frontend/src/pages/__tests__/Settings.test.jsx` both exist,
and all 12 of AUTH-06's own TCs independently PASS (see `docs/features/AUTH-06/VALIDATION-
20260818-1300.md`).

**Re-verification performed:** read `frontend/src/pages/__tests__/Settings.test.jsx` in full
(214 lines, 12 `it(...)` blocks, one per AUTH-06-TC-01..TC-12) and cross-checked against
AUTH-07-TC-11's `then` clause: "logout proceeds and after the redirect, focus is moved to the
Login screen's primary input field," triggered by "user tabs to the Logout control and activates
it with Enter/Space."

Grep for keyboard/focus terminology (`keyboard|focus|tabIndex|Tab\b|activeElement`) against the
file returned **zero matches**. Manual line-by-line reading of all 12 tests confirms:

- TC-04/TC-05 (the two tests closest to TC-11's subject, the Logout button) both fire the click
  handler via `fireEvent.click(...)` — no `userEvent.tab()`, no `keyboard.press("Enter"/" ")`, no
  assertion on `document.activeElement`.
- TC-07/TC-08/TC-12 (AUTH-06's own accessibility-labelled TCs) assert spinner presence, persistent
  `role="alert"` banner, and `details-item` CSS class structure — none touch keyboard interaction
  or focus.
- No test in the file mounts anything beyond `<Settings />` in isolation — there is no Login
  screen, no router, and no `App.jsx`-level state to redirect into or receive focus. Confirmed via
  the file's own imports (only `Settings.jsx` and `userApi.js` are imported).

Cross-checked the other three App-level test files in the frontend suite
(`App.auth.test.jsx`, `App.authGate.test.jsx`, `App.routing.test.jsx`, `App.auth.perf.test.jsx`)
for any focus-after-logout-redirect assertion — none found; those suites test auth-gating and
routing state transitions, not focus management.

**Verdict: gap NOT closed.** The AUTH-06/AUTH-07 PLAN.md § 7 attribution ("TC-11 ... is authored
and owned entirely by AUTH-06's already-existing test file") turns out to be inaccurate as
implemented — AUTH-06's PLAN.md § 7 itself only ever assigned TC-07/TC-08/TC-12 (spinner/banner/
label-class) to the accessibility row for `Settings.test.jsx`, never a keyboard/focus TC. AUTH-06
never had its own TC for keyboard-operability or post-redirect focus, so there was nothing for
its test file to "already" cover. This is a genuine coverage gap, not a resolved sequencing
blocker: even with AUTH-06 fully implemented, no test anywhere in the frontend suite exercises
keyboard activation of Logout or focus moving to Login after redirect. Closing it needs a new
App.jsx-level test (mount authenticated app → keyboard-activate Logout → assert `isAuthenticated`
flips → assert Login's primary input receives focus), which is a new task not currently owned by
either story's PLAN.

`docs/test-cases/AUTH-07.json` TC-11's `last_run` block and `docs/features/AUTH-07/state.json`'s
`pending_carry_forward` entry have both been updated (tag changed from `sequencing` to
`coverage-gap`) to reflect this precisely — the carry-forward is NOT resolved, only re-diagnosed.

### Passed

| TC id | Title |
|-------|-------|
| AUTH-07-TC-01 | Settings-screen click-through logout (declared e2e, module-level assertions) |
| AUTH-07-TC-02 | clearAuthToken() called exactly once, auth_token key removed |
| AUTH-07-TC-03 | Zero HTTP requests, synchronous undefined return |
| AUTH-07-TC-04 | Post-logout protected-route redirect (no residual token/header) |
| AUTH-07-TC-05 | No residual localStorage/sessionStorage/cookie artifacts |
| AUTH-07-TC-06 | console.log asserts exact `{ timestamp }` payload, no PII |
| AUTH-07-TC-07 | Double-invocation idempotency, no thrown errors |
| AUTH-07-TC-08 | logout() still completes/logs when clearAuthToken's removeItem throws |
| AUTH-07-TC-09 | In-flight-request race: sync clear + eventual 401 via existing dedup-guarded interceptor |
| AUTH-07-TC-10 | logout() p95 latency < 10ms across 100+ iterations |

Independently re-run via `cd frontend && npm run test`: `userApi.logout.test.js (10 tests)` all
PASS (16ms), consistent with round 2's recorded results.

### Flaky tests

None.

### Performance budget breaches

None. AUTH-07-TC-10's budget (p95 < 10ms) — recorded as `budget_pass: true` in round 2 and
unchanged this round (TC-10 was not in scope for round-3 re-verification).

### Regression coverage

None. No TC in `docs/test-cases/AUTH-07.json` carries a `regression-*` tag.

### Failed (bug blocks)

### AUTH-07-TC-11 — Logout control is keyboard-operable and focus moves to Login's primary field after redirect (Priority: Should)

- AC: Given an authenticated user on the Settings screen using keyboard only, when they tab to
  the Logout control and activate it with Enter/Space, then logout proceeds and focus moves to
  the Login screen's primary input field after the redirect.
- Attempts: 1 (ERROR — no test exists to run)
- Steps executed: read `frontend/src/pages/__tests__/Settings.test.jsx` in full; grepped for
  keyboard/focus terms; cross-checked all 4 App-level test files.
- Expected: a passing automated test asserting keyboard activation + post-redirect focus move.
- Actual: no such test exists anywhere in the frontend suite.
- Failure reason: coverage gap — AUTH-06's Settings.test.jsx was never scoped (by either story's
  own PLAN.md § 7) to include a keyboard/focus TC; the PLAN's TC-11 attribution to that file is
  inaccurate as implemented.
- Artefact: none (no flow/test file to point to).
- Likely cause / recommended action: add a new task (either an AUTH-06 or AUTH-07 follow-up, or a
  dedicated AUTH-08+ story) for an App.jsx-level integration test covering keyboard-activated
  Logout and post-redirect focus management. This is a `Should`-priority TC, so it does not block
  merge on its own but must not be silently dropped — tracked via the updated
  `pending_carry_forward` entry in `docs/features/AUTH-07/state.json`.

### Errored

None (TC-11 recorded as FAIL/no-coverage rather than a runtime ERROR, since no flow was ever
generated to error).

### Manual follow-up

None currently — TC-11 is `automatable: true` and remains a tracked coverage gap (see above)
rather than a manual-only TC.

### Proof of run

```
### Proof of run

- session_id:        (validation-agent subagent session, not separately surfaced by harness)
- bash_invocations:  14 (shared with AUTH-06's pass in the same session; see AUTH-06's report)
- test_runner_runs:  4 (frontend npm run test x1, backend pytest x1, frontend npm run build x1, plus preflight smoke-import x1)
- stack_smoke_runs:  2 (react start+healthcheck, fastapi start+healthcheck — shared with AUTH-06's pass)
- duration_seconds:  ~620 (combined AUTH-06 + AUTH-07 session)
- timestamp:         2026-08-18T13:00:00Z
```

### Verdict justification

PARTIAL — AUTH-07-TC-11 (`Should` priority) has `verdict: FAIL` per the updated
`docs/test-cases/AUTH-07.json` last_run block. All other 10 TCs, all 5 PLAN tasks, and both
stack-smoke entries PASS. Per the verdict rule ("at least one automatable TC has `verdict: FAIL`"
→ PARTIAL), this story cannot be marked PASS until TC-11 is either (a) closed with a genuine
App-level keyboard/focus test, or (b) explicitly re-scoped/de-prioritized via a PRD/PLAN amendment
with product-owner sign-off.
