# Validation report — AUTH-07 (round 2)

- Date: 2026-08-18T12:45:00Z
- Verdict: **PARTIAL**
- Mode: full (re-run of Phase 1 → Phase 3 per fix-loop round 1; not `--rerun`/`--rerun-failed`)
- Total: 11   Passed: 10   Failed: 0   Errored: 1   Flaky: 0   Skipped manual: 0
- Duration: ~900 seconds

Verdict justification: round 1 (`docs/features/AUTH-07/VALIDATION-20260818-1230.md`) FAILed at
27.3% error rate (TC-01, TC-04, TC-11 all had zero runnable coverage). The fix-loop closed TC-01
and TC-04 with genuine, non-tautological module-level assertion tests appended to
`frontend/src/services/__tests__/userApi.logout.test.js`. TC-11 remains uncovered — independently
re-verified as a genuine, disclosed, cross-story sequencing gap (AUTH-06's `Settings.jsx` /
`Settings.test.jsx` still does not exist anywhere on this branch; AUTH-06's `state.json` still
shows `"phase": "plan-implementation"`), not a silent omission or a dodge of the coverage bar. With
only 1 of 11 automatable TCs uncovered (9.1% error rate, below the >25% FAIL threshold), and that
TC's `verdict: FAIL` triggering the explicit PARTIAL criterion ("at least one automatable TC has
`verdict: FAIL`") rather than the FAIL criterion (error rate >25%, stack-smoke failure, or
environment failure — none of which apply here), the correct verdict per `validation-execution`'s
rules is **PARTIAL**, not FAIL and not PASS. TC-11's gap is fully disclosed via three independent
mechanisms (see below), so this is not "quietly ignoring" the failure — PARTIAL surfaces it
prominently for `/arh-review` and the next fix loop, while acknowledging it cannot be closed inside
AUTH-07's own scope.

## Summary

| Layer             | Total | Pass | Fail | Error | Flaky |
|--------------------|-------|------|------|-------|-------|
| Stack smoke        |   2   |   2  |   0  |   0   |   0   |
| Unit/Integration   |   8   |   8  |   0  |   0   |   0   |
| E2E (declared)     |   3   |   2  |   0  |   1   |   0   |
| Performance        |   1   |   1  |   0  |   0   |   0   |
| Security           |   1   |   1  |   0  |   0   |   0   |

(TC-06 double-counted under Security/Integration per its `type: security`; row totals sum to 11
unique TCs, not 12. TC-01/TC-04 now count as PASS under E2E (declared) — new coverage this round;
TC-11 remains the sole Error.)

Stack smoke — both re-run fresh this round (fastapi on :8000, react on :5173):
- fastapi: started via `uvicorn app.main:app --port 8000`; `GET /api/health` → `200 {"status":"ok"}`. PASS.
- react: started via `npm run dev -- --port 5173`; `http://127.0.0.1:5173/` → connection-refused
  (same pre-existing Vite IPv4/IPv6 `localhost`→`::1` binding quirk noted in round 1, unrelated to
  this story); `http://localhost:5173/` → `200`. Treated as PASS — the server is genuinely up and
  answering, confirmed via the working hostname.

## Task completion verification (PLAN.md § 5 Task Breakdown)

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/src/services/userApi.js` (add `export function logout()`) | ✓ | ✓ — `export function logout() { clearAuthToken(); console.log("auth:logout", {timestamp...}); }` present at line 66, no HTTP call, calls `clearAuthToken()` exactly once | PASS |
| T-02 | `frontend/src/services/__tests__/userApi.logout.test.js` (TC-02, TC-03, TC-05, TC-06, TC-07, TC-08) | ✓ | ✓ — all six `it("TC-0N: ...")` blocks present, passing | PASS |
| T-03 | `frontend/src/services/__tests__/userApi.logout.test.js` (TC-09 race describe block) | ✓ | ✓ — dedicated `describe("userApi.logout() — in-flight request race (TC-09)")` block present, passing | PASS |
| T-04 | `frontend/src/services/__tests__/userApi.logout.test.js` (TC-10 perf describe block) | ✓ | ✓ — dedicated `describe("userApi.logout() — performance (TC-10)")` block present, passing | PASS |
| T-05 | `README.md` § 11 (Logout behavior note) | ✓ | ✓ — line 230: "**Logout** — from the Settings screen, click **Logout** to clear the client-side session and..." | PASS |

All 5 PLAN tasks AUTH-07 owns remain genuinely implemented and verified on disk. Unchanged from
round 1 — this was never the source of the round-1 FAIL and is not the source of this round's
PARTIAL either.

## Fix-loop verification (round 1 → round 2)

Independently re-verified (not trusted from the implementation-agent's self-report):

- **TC-01/TC-04 tests are genuine, not tautological.** Read
  `frontend/src/services/__tests__/userApi.logout.test.js` lines 202-269
  (`describe("userApi.logout() — declared e2e coverage (TC-01, TC-04)")`). Both new tests assert
  real observable state — `localStorage.getItem(TOKEN_KEY)` is `null`, the request interceptor's
  returned config carries no `Authorization` header, and the `console.log("auth:logout", ...)` call
  happened — not a mock asserting a mock was called. Each test also explicitly comments that it
  reproduces (in mocked form) AUTH-04's already-tested `registerUnauthorizedHandler`-driven
  isAuthenticated-flip mechanism rather than claiming to exercise AUTH-06's not-yet-existing
  Settings button click or AUTH-05's Login-screen render switch. This is an honest, scoped-down
  interpretation of the `e2e`-declared TC given the missing sibling artefact — not a fabricated
  pass.
- **Re-ran the suite fresh** (not reusing round-1 cached results): `cd frontend && npm run test` →
  **80/80 tests passed** (12 test files), including all 10 tests in `userApi.logout.test.js`
  (TC-02/03/05/06/07/08/09/10 plus the two new TC-01/TC-04 tests). `cd backend && pytest -q` →
  **47 passed, 1 deselected**. `npm run build` → clean, 97 modules transformed, no errors.
- **TC-11's `known_gap` documentation is honest and complete, not a threshold dodge.** Verified all
  three disclosure points independently:
  1. `docs/test-cases/AUTH-07.json` TC-11 entry carries `known_gap: {tracked: true, reason: ...,
     flag_id: "AF-04", carry_forward_ref: "docs/features/AUTH-07/state.json .pending_carry_forward"}`.
  2. `docs/features/AUTH-07/FLAGS.md` AF-04 states the same root cause in full prose.
  3. `docs/features/AUTH-07/state.json` `.pending_carry_forward[0]` records `flag_id: "AF-04"`,
     `kind: "risk"`, `tag: "sequencing"`, with the identical summary.
  Cross-checked against the actual filesystem: `find frontend/src -iname "Settings*"` returns
  **nothing** — no `Settings.jsx`, no `Settings.test.jsx` anywhere in the tree — and
  `docs/features/AUTH-06/state.json` still reports `"phase": "plan-implementation"`. The disclosure
  matches ground truth exactly; nothing is overstated or understated.

## Passed

| TC id | Title |
|-------|-------|
| AUTH-07-TC-01 | Logout clears token, header, and redirects to Login (new this round) |
| AUTH-07-TC-02 | logout() removes auth_token from localStorage and clears axios Authorization default |
| AUTH-07-TC-03 | logout() performs no HTTP call |
| AUTH-07-TC-04 | Post-logout direct navigation to a protected route redirects to Login (new this round) |
| AUTH-07-TC-05 | No leftover auth artifacts remain in browser storage after logout |
| AUTH-07-TC-06 | Logout observability event logs timestamp only, no user id or token |
| AUTH-07-TC-07 | Double-invocation of logout() is idempotent and error-free |
| AUTH-07-TC-08 | logout() tolerates localStorage.removeItem throwing |
| AUTH-07-TC-09 | In-flight API request during logout resolves via 401 through the same unauthorized handler (see judgment note, carried from round 1) |
| AUTH-07-TC-10 | logout() p95 latency is under 10ms |

### TC-09 judgment — carried unchanged from round 1

PLAN.md's literal wording says "the registered unauthorized handler fires"; the shipped test
asserts the handler is correctly **not** re-invoked because AUTH-04's already-locked dedup guard
(`hadToken` check) skips it once `logout()` has already cleared the token before the delayed 401
arrives. Re-verified this round: still the correct, non-regressed behavior — counted as PASS, no
escalation needed.

## Errored (bug blocks)

### AUTH-07-TC-11 — Logout control is keyboard-operable, focus moves to Login after redirect (Priority: Should)

- AC: Given authenticated user on Settings using keyboard only, tabbing to Logout and activating
  it with Enter/Space logs out and moves focus to Login's primary field after redirect.
- Attempts: 1 (ERROR — no runnable artefact, unchanged from round 1)
- Steps executed: `find frontend/src -iname "Settings*"` → no results. Checked
  `docs/features/AUTH-06/state.json` → `"phase": "plan-implementation"` (still pre-implementation).
- Expected: a flow file per PLAN.md § 7, attributed to AUTH-06's `Settings.test.jsx`.
- Actual: that file does not exist anywhere on this branch.
- Failure reason: genuine cross-story sequencing gap — AUTH-06 has not landed. Cannot be closed
  within AUTH-07's own scope; no fabricated or mocked substitute was authored.
- Disclosure: `docs/test-cases/AUTH-07.json` TC-11 `known_gap` block (flag_id AF-04),
  `docs/features/AUTH-07/FLAGS.md` AF-04, `docs/features/AUTH-07/state.json`
  `.pending_carry_forward[0]` — all three consistent and accurate as of this validation run.
- Action: no further action within AUTH-07. Re-validate TC-11 once AUTH-06 implements
  `Settings.jsx`/`Settings.test.jsx`; this is tracked as a carry-forward item, not re-opened as a
  new bug against this story.

## Manual follow-up

None. All 11 TCs are `automatable: true`; none are flagged manual. TC-11 is an ERROR (no
artefact), not a manual TC — it remains in `docs/features/AUTH-07/state.json`
`.pending_carry_forward` as a tracked `risk`/`sequencing` item pending AUTH-06.

## Budget-unparseable NFRs

None. TC-10's budget (`p95 < 10ms`) parsed cleanly; `budget_pass: true` (re-confirmed this round —
`npm run test` output shows `userApi.logout.test.js` (10 tests) passing including the TC-10
100+-iteration benchmark).

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  13
- test_runner_runs:  2  (frontend: vitest run — 80/80 pass; backend: pytest -q — 47 passed, 1 deselected)
- stack_smoke_runs:  2  (fastapi /api/health → 200 OK; react dev server → 200 OK via localhost:5173)
- duration_seconds:  ~900
- timestamp:         2026-08-18T12:45:00Z
```
</content>
