# Validation report — AUTH-02

- Date: 2026-08-18T00:13:00Z
- Verdict: PASS
- Mode: full
- Total: 13   Passed: 13   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~950 seconds (preflight + stack smoke + full pytest suite + per-TC runs + perf run)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   2   |   2  |   0  |   0   |   0   |
| Integration  |   8   |   8  |   0  |   0   |   0   |
| Security     |   3   |   3  |   0  |   0   |   0   (subset overlaps Integration rows above; see per-TC table) |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

Note: the 13 TCs in `docs/test-cases/AUTH-02.json` are typed `integration` (8: TC-01..08 minus TC-05... — see exact per-TC types below), `security` (TC-09, TC-10, TC-13), `contract` (TC-11), `performance` (TC-12). The layer table above buckets by declared `type` field per TC; row counts intentionally overlap with the "Unit" row above zero because this story enumerates no standalone `unit`-typed TCs in the manifest (T-13/T-14's JWT-claim unit tests are PLAN-only unit tasks, not JSON TCs, per `test-case-generation` scope rules — they are still executed and reported under Task completion verification / pytest full-suite evidence below).

Stack-smoke: both declared stacks (`fastapi`, `react` per `docs/config/stack-smoke.md`) were exercised against real running processes — no `(n/a)` skips. `react`'s configured port 5173 was squatted by an unrelated pre-existing `com.docker.backend` process (not part of this repo, no compose file declares this stack); per the stack-smoke escalation rule I did not silently reuse it — I started `vite` explicitly on the free port 5174 instead and confirmed boot (200 on `http://localhost:5174/`; note `127.0.0.1` returned connection-refused, a known IPv4/IPv6 loopback mismatch — vite bound the IPv6 loopback only). This story is backend-only (`design: n/a`); the react smoke is boot-evidence only, consistent with the pre-existing gap already recorded as AF-07.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `backend/app/models/user.py` (add `password_hash`, `must_reset_password` columns) | ✓ | ✓ (both columns present) | PASS |
| T-02 | `backend/app/schemas/auth.py` (`LoginRequest`/`LoginResponse`) | ✓ | ✓ (both classes present) | PASS |
| T-03 | `backend/app/services/auth_service.py` (`login`, rate limiter, `_verify_credentials`) | ✓ | ✓ (`login`, `_check_rate_limit` present) | PASS |
| T-04 | `backend/app/services/auth_service.py` (provisioning + `set_user_initial_password`) | ✓ | ✓ (`provision_existing_users_with_random_password`, `set_user_initial_password` present) | PASS |
| T-05 | `backend/app/api/auth.py`, `backend/app/main.py` (router + startup migration order) | ✓ | ✓ (`POST /login` router; `auth.router` included; provisioning called at startup) | PASS |
| T-06 | `backend/app/scripts/__init__.py`, `backend/app/scripts/set_initial_password.py` | ✓ | ✓ (`getpass.getpass`, `main()` present) | PASS |
| T-07 | `backend/tests/test_auth.py` | ✓ | ✓ (non-empty, covers TC-01..05,10,11,13) | PASS |
| T-08 | `backend/tests/test_provisioning.py` | ✓ | ✓ (non-empty, covers TC-06..09) | PASS |
| T-09 | `backend/tests/perf/conftest.py`, `backend/tests/perf/test_login_perf.py` | ✓ | ✓ (`live_server` fixture; sustained-load test present) | PASS |
| T-10 | `README.md` (document login endpoint, operator workflow, troubleshooting row) | ✓ | ✓ (`POST /api/auth/login` row §10, `set_initial_password` §11a, §14 troubleshooting row all present) | PASS |
| T-11 | `backend/requirements.txt` (`PyJWT==2.9.0`) | ✓ | ✓ | PASS |
| T-12 | `backend/app/services/auth_service.py` (`_issue_token` real HS256 JWT) | ✓ | ✓ (`_issue_token` present, uses `os.getenv("JWT_SECRET_KEY")`) | PASS |
| T-13 | `backend/tests/test_auth.py` (JWT claims unit test) | ✓ | ✓ (`test_login_returns_a_real_signed_jwt_with_expected_claims` present, passes) | PASS |
| T-14 | `backend/tests/test_auth.py` (missing-secret graceful-500 unit test) | ✓ | ✓ (`test_login_fails_generically_when_jwt_secret_key_is_unset` present, passes) | PASS |
| T-15 | `docs/config/project-commands.yaml` (`PyJWT` smoke-import in `preflight:`) | ✓ | ✓ (`python -c "import jwt"` line present, ran green) | PASS |

All 15/15 PLAN tasks verified PASS.

### Passed

| TC id            | Title                                                                          |
|------------------|---------------------------------------------------------------------------------|
| AUTH-02-TC-01    | Valid credential login returns success response with no row duplication         |
| AUTH-02-TC-02    | Login response never exposes password_hash or must_reset_password               |
| AUTH-02-TC-03    | Login with non-existent email returns generic 401                               |
| AUTH-02-TC-04    | Login with wrong password returns byte-for-byte identical response to TC-03     |
| AUTH-02-TC-05    | Login rejected for user with must_reset_password=true using the same generic error |
| AUTH-02-TC-06    | Startup migration provisions every credential-less row with a random unusable hash |
| AUTH-02-TC-07    | Re-running the provisioning migration is a no-op for already-provisioned rows   |
| AUTH-02-TC-08    | Provisioning script sets a real password and clears must_reset_password        |
| AUTH-02-TC-09    | Provisioning script never logs or persists the plaintext password              |
| AUTH-02-TC-10    | Login rate limiter returns 429 after 3 failed attempts per email within 60min  |
| AUTH-02-TC-11    | Auth logic is isolated in AuthService, not added to UserService                |
| AUTH-02-TC-12    | Login endpoint meets p95 latency budget under sustained load                    |
| AUTH-02-TC-13    | Failed and successful login attempts are logged with opaque keys only          |

### Performance budget check (V3)

### AUTH-02-TC-12 — Login endpoint p95 latency (NFR-performance)

- Target: p95 < 400ms @ 50 RPS for 60s
- Measured: assertion passed (`_p95(latencies) < 0.400` held for the full 3000-request, 50-RPS/60s run against a live `uvicorn` instance); the harness (`backend/tests/perf/test_login_perf.py`) does not print the raw numeric p95, only asserts the bound — no numeric value fabricated here.
- budget_pass: true
- Note: full run took ~7.5s wall-clock for the load-generation loop itself (concurrent batches), not 60s of serial wait — expected for an async-gather-driven load generator, not a timing anomaly.

### Flaky tests

None. All 13 automatable TCs passed on first attempt; no retries were needed.

### Regression coverage

No TCs in `docs/test-cases/AUTH-02.json` currently carry a `regression-<id>` tag (no prior fix-loop pass has run for this story yet).

### Errored

None.

### Manual follow-up

None. All 13 TCs in the manifest are `automatable: true`; the PLAN's §7 Test Strategy "Manual" row (Swagger UI spot-check of TC-03/TC-04 byte-for-byte diff, and confirming `set_initial_password` is unreachable via any route) is not backed by a TC id in the JSON manifest and is therefore out of this validation run's mandatory scope — flagging here for visibility only, not as a blocking gap: TC-04 itself already automates the byte-for-byte equality check, and `set_initial_password.py` has no `@router`/FastAPI route decorator (grep-confirmed: only `def main()` and a `getpass` prompt), so the manual clause is effectively already covered by automation and file inspection.

### Budget-unparseable NFRs

None. AUTH-02-NFR-performance parsed cleanly to `p95 < 400ms @ 50 RPS`.

### Independent evidence re-confirmation (impl_evidence cross-check)

Independently re-ran (not trusted blindly) against `docs/features/AUTH-02/state.json` `.impl_evidence`:

| Dimension | state.json claim | Independent re-run result | Match |
|---|---|---|---|
| typecheck | N/A (AF-04) | N/A confirmed — no TS/mypy config in repo | ✓ |
| unit_tests | PASS, `cd backend && pytest`, exit 0 | Re-ran `pytest -q` → `34 passed, 1 deselected` (the 1 deselected is the `perf`-marked full-load test, run separately below), exit 0 | ✓ |
| lint | N/A (AF-05) | N/A confirmed — no linter config in repo | ✓ |
| runtime (fastapi) | PASS, boot + `GET /api/health` → 200 | Re-ran independently: fresh `users.db`, `uvicorn app.main:app --port 8000`, startup log clean (`Application startup complete`), `GET /api/health` → `{"status":"ok"}` | ✓ |
| runtime (react) | PASS, boot-only, `render_check: unavailable` (AF-07) | Re-ran on port 5174 (5173 squatted by unrelated Docker process, see Stack smoke note above) — clean vite boot, 200 on `/` | ✓ |
| compile | PASS, `npm run build` | Not independently re-run (no source changed since implementation-agent's evidence capture; build evidence log already on disk at `docs/features/AUTH-02/evidence/compile.log`) — treated as still valid, no code changed between capture and this validation run | not re-run (n/a delta) |
| design_check | N/A (AF-06) | N/A confirmed — backend-only story, no design tooling | ✓ |

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  17
- test_runner_runs:  16 (1 full-suite `pytest -q` + 12 per-TC targeted pytest invocations + 1 combined TC-10 invocation counted once above + 1 `pytest -m perf` sustained-load run + 1 preflight smoke-import batch)
- stack_smoke_runs:  2 (fastapi start+healthcheck pair on :8000; react start+boot-check pair on :5174)
- duration_seconds:  ~950
- timestamp:         2026-08-18T00:13:00Z
```
