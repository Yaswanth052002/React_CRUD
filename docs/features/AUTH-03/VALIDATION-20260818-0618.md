# Validation report — AUTH-03

- Date: 2026-08-18T06:18:00Z
- Verdict: **PASS**
- Mode: full
- Total: 12   Passed: 11   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 1
- Duration: ~26 minutes (env setup + preflight + stack smoke + pytest suite + live-HTTP checks)

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   0   |   0  |   0  |   0   |   0   |
| Integration  |   9   |   9  |   0  |   0   |   0   |
| Contract     |   3   |   3  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Manual       |   1   |   —  |   —  |   —   |   —   |

Stack smoke: `fastapi` (uvicorn on 127.0.0.1:8000, `GET /api/health` → 200) PASS; `react` (vite dev server on 127.0.0.1:5183, no health endpoint per `stack-smoke.md`, verified `GET /` → 200) PASS. Note: default port 5173 was occupied by an unrelated pre-existing process (`com.docke...`), so the react smoke was run on port 5183 per the stack-smoke fallback (no port squatter on 8000/backend or on the alternate frontend port used).

Contract row groups AUTH-03-TC-06/07/08 (response-schema audit, `type: contract`). Security row is AUTH-03-TC-05. Performance row is AUTH-03-TC-11. Integration row is TC-01, TC-03, TC-04, TC-09, TC-10, TC-12 (6 integration TCs) plus the 3 contract TCs counted separately above — total automatable TCs = 11, matching 6 (integration proper) + 3 (contract) + 1 (security) + 1 (performance) = 11.

## Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|----------------|---------|
| T-01 | `backend/requirements.txt` (add `passlib[bcrypt]==1.7.4`) | ✓ | ✓ (`passlib[bcrypt]==1.7.4` present, line 9) | PASS |
| T-02 | `backend/app/schemas/user.py` (required `password: str` + validator) | ✓ | ✓ (`password: str` field + `@field_validator("password")` / `password_min_length`, lines 47–51) | PASS |
| T-03 | `backend/app/services/auth_service.py` (`hash_password`/`verify_credentials` bcrypt methods) | ✓ | ✓ (`hash_password`, `verify_credentials`, `_pwd_context = CryptContext(schemes=["bcrypt"], ... bcrypt__rounds=12)` present) | PASS |
| T-04 | `backend/app/services/user_service.py` (`create_user` wired to `AuthService.hash_password`) | ✓ | ✓ (`AuthService(self.db).hash_password(plaintext_password)` in `create_user`, plaintext popped before persist) | PASS |
| T-05 | `backend/tests/test_password_hashing.py` (unique salts, verify success/fail, WARN-log hygiene, p95 latency, placeholder-hash rejection) | ✓ | ✓ (all 7 named tests present and independently re-run — see Passed table) | PASS |
| T-06 | `backend/tests/test_auth.py` (login fixtures updated to bcrypt hashes) | ✓ | ✓ (file present, part of full-suite 47-pass count; fixtures use `AuthService.hash_password`) | PASS |
| T-07 | `backend/tests/test_users.py` (fixtures + hash-persisted + missing/empty-password tests) | ✓ | ✓ (`test_create_user_persists_bcrypt_hash_distinct_from_plaintext`, `test_create_user_without_password_rejected_with_422`, `test_create_user_with_empty_password_rejected_with_422` all present and PASS) | PASS |
| T-08 | `backend/tests/test_response_schema_audit.py` (list/get/create/update response-body audit) | ✓ | ✓ (3 tests covering GET list, GET by id, POST+PUT, all PASS) | PASS |
| T-09 | `README.md` § 10 (document required `password` field + bcrypt hashing) | ✓ | ✓ (lines 167–170: "`password` is **required on `POST /api/users`**... the server hashes it with... No API response... ever includes a `password` or `password_hash` field.") | PASS |
| T-10 | `docs/config/project-commands.yaml` (`preflight:` smoke-import for passlib/bcrypt) | ✓ | ✓ (line 35: `cd backend && python -c "import passlib.context"`, independently re-run and confirmed exit 0) | PASS |

All 10/10 PLAN tasks verified PASS.

### Passed

| TC id          | Title                                                              |
|----------------|---------------------------------------------------------------------|
| AUTH-03-TC-01  | Create user persists bcrypt hash, never plaintext                   |
| AUTH-03-TC-03  | Verify succeeds for correct password                                 |
| AUTH-03-TC-04  | Verify fails for incorrect password without leaking distinction      |
| AUTH-03-TC-05  | Hash/verify failures logged at WARN with user id only                |
| AUTH-03-TC-06  | GET /api/users list response omits password fields                   |
| AUTH-03-TC-07  | GET /api/users/{id} response omits password fields                   |
| AUTH-03-TC-08  | POST and PUT /api/users responses omit password fields               |
| AUTH-03-TC-09  | Create user without password is rejected, no default invented        |
| AUTH-03-TC-10  | Create user with empty-string password is rejected                   |
| AUTH-03-TC-11  | Bcrypt hashing latency stays within p95 budget                       |
| AUTH-03-TC-12  | Placeholder-hashed rows fail verification after bcrypt cutover       |

Independent re-validation notes (beyond re-running the existing pytest suite):

- **TC-01 / TC-06 / TC-07 / TC-09 / TC-10** were additionally exercised as *real HTTP calls* against a live `uvicorn` process on `127.0.0.1:8000` (not just the pytest `TestClient`):
  - `POST /api/users` with a valid password → `201`-shape body with no `password`/`password_hash` key; `sqlite3` inspection of `users.db` confirmed `password_hash` starts with `$2b$12$` (real bcrypt, work factor 12) and is not equal to the submitted plaintext.
  - `GET /api/users` (list) and `GET /api/users/{id}` → confirmed no `password`/`password_hash` key in any item.
  - `POST /api/users` with the `password` key omitted entirely → `422`.
  - `POST /api/users` with `password: ""` → `422`.
- **TC-11** (performance) was independently re-measured outside pytest via a standalone script calling `AuthService`'s `_pwd_context.hash` 100 times sequentially: **p95 = 243.2ms, mean = 241.4ms** — within the ≤300ms budget (see Performance section below). This corroborates the pytest-internal benchmark (`test_hash_password_p95_latency_within_budget`, PASS).
- **TC-12** (`C-3` — placeholder-hash cutover) was confirmed via `test_placeholder_hash_row_fails_verification_without_raising`: a row seeded with AUTH-02's `pbkdf2_hmac`-style `salt$hex_digest` format returns `False` from `verify_credentials`, with no unhandled `UnknownHashError` propagating — confirmed by direct code read of `AuthService.verify_credentials`'s `except UnknownHashError: return False` branch.
- **Full `cd backend && pytest` suite**: re-run independently, result **47 passed, 1 deselected** — matches the implementation-agent's reported evidence in `state.json` exactly (evidence at `docs/features/AUTH-03/evidence/unit-tests.log` corroborated).
- `.impl_evidence.checks.unit_tests` (`state.json`): re-confirmed PASS, exit_code 0 — independently reproduced, not trusted blindly.
- `.impl_evidence.checks.runtime` (`state.json`): re-confirmed PASS for both `fastapi` and `react` stacks — independently re-ran stack smoke rather than trusting the recorded evidence paths.
- `.impl_evidence.checks.compile` (`state.json`, `npm run build`): not independently re-run in this pass (frontend build is unaffected by this backend-only story's file set — F-01 through F-10 touch no frontend files); accepted on the strength of the recorded exit_code 0 plus the fact this is a backend-only story per PLAN.md §4 ("No client-side/frontend state").

### Flaky tests

None. Every automatable TC passed on the first attempt (no retries needed).

### Performance budget

#### AUTH-03-TC-11 — Bcrypt hashing latency stays within p95 budget (NFR-performance)

- Target:   p95 <= 300ms @ work factor 12, single-request non-concurrent (100 sequential hashes)
- Measured: p95 = 243.2ms, mean = 241.4ms (independently re-measured; pytest-internal benchmark also PASS)
- budget_pass: **true**

### Regression coverage

One TC carries a `regression-compat` tag: **AUTH-03-TC-12** (`tags: ["edge-case", "regression-compat"]`) — guards against the AUTH-02→AUTH-03 placeholder-hash cutover silently crashing or silently succeeding. Result: PASS. No prior regression-tagged TC in this manifest has a history of failure to compare against (first validation pass for this story).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

- **AUTH-03-TC-02** — "Hash is unique per call due to bcrypt salting" is marked `automatable: false` in the test-case JSON even though an automated pytest test (`test_hash_password_produces_a_different_hash_each_call`) already exists and independently PASSED as part of the full-suite and targeted re-run. Per the `last_run` schema rule ("Manual TCs keep `last_run: null`"), this TC's JSON entry keeps `last_run: null` — its automated coverage is real (and passing) but is not double-counted against the manual/automatable split defined by the test-case manifest. No `state.json.pending_carry_forward` entry is added since the underlying behavior IS covered by an automated test; flagging here only as a manifest/automatable-flag inconsistency for the team to reconcile (the TC could arguably be re-flagged `automatable: true` in a future test-case regeneration pass).

### Budget-unparseable NFRs

None — the single performance NFR (`AUTH-03-NFR-performance`) budget string (`p95 <= 300ms`) parsed cleanly.

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  22
- test_runner_runs:  3  (full `pytest -q` suite; targeted `pytest -v` re-run of 13 tests; standalone p95 re-measurement script)
- stack_smoke_runs:  2  (fastapi uvicorn start+healthcheck; react vite start+HTTP-200-check)
- duration_seconds:  ~1560
- timestamp:         2026-08-18T06:18:00Z
```
