# Validation report — AUTH-01 (round 2)

- Date: 2026-08-18T14:45:00Z
- Verdict: PASS
- Mode: rerun
- Total: 9   Passed: 9   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: 340 seconds

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   0   |   0  |   0  |   0   |   0   |
| Integration  |   7   |   7  |   0  |   0   |   0   |
| E2E          |   0   |   0  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |

Notes:
- This is a re-run following the round-1 fix loop's single finding: AUTH-01-TC-05's original assertion (`form.scrollWidth <= form.clientWidth`) was tautological in jsdom (always `0 <= 0`). Fix (recorded as AF-05 in `docs/features/AUTH-01/FLAGS.md`) replaced it with a structural, non-tautological check in `frontend/src/components/__tests__/LoginForm.test.jsx:70-88`.
- The two TCs declared `type: e2e` (TC-05, TC-08) continue to execute as Vitest/jsdom assertions per `PLAN.md § 7 Test Strategy` — counted under Integration above, same as round 1 (`test_e2e: n/a`).
- Backend `pytest` suite (16 tests) re-ran and passed, confirming AUTH-01's frontend-only fix does not affect the backend.
- Stack smoke: `react` dev server started with `--host 127.0.0.1 --port 5173` (fixing round 1's IPv4/IPv6 mismatch note) and `fastapi` backend (port 8000) both started clean and answered health checks with `200`.

### Independent verification of the TC-05 fix (this validation round, not self-declared by the fix)

1. **Non-tautology check** — read `frontend/src/components/__tests__/LoginForm.test.jsx:70-88`. The test now (a) scans all `[style]`-bearing elements LoginForm renders for a fixed inline px width >320px, and (b) reads `frontend/src/styles/index.css` at runtime, regex-extracts the `.form-input, .form-select` rule body, and asserts it is `width: 100%` with no fixed `(min-)?width: \d{3,}px`. Neither assertion is a jsdom-computed-layout call (no `scrollWidth`/`clientWidth`), so the jsdom-always-zero failure mode from round 1 does not apply.
2. **Empirical proof the assertion is load-bearing** — mutated `frontend/src/styles/index.css`'s `.form-input`/`.form-select` rule from `width: 100%` to `width: 500px` and re-ran TC-05 in isolation (`npx vitest run ... -t "TC-05"`). Result: **FAIL** — `expected '\n  width: 500px;...' to match /width:\s*100%/`. This proves the test would genuinely catch a real 320px-overflow regression, unlike the round-1 tautology. Reverted the mutation and diffed the file back to its original content before the final full-suite run (confirmed identical, zero residual change).
3. **CSS accuracy spot-check** — read `frontend/src/styles/index.css:1117-1126` directly: `.form-input, .form-select { width: 100%; padding: 10px 12px; border-radius: var(--radius-md); border: 1px solid var(--border-strong); background: var(--surface); font-size: 13.5px; color: var(--ink); }`. Matches exactly what the test's regex extracts and asserts against — no discrepancy between the test's claim and the actual CSS.
4. **AF-05 disclosure accuracy** — re-read `docs/features/AUTH-01/FLAGS.md` AF-05. It correctly states this is a structural/proxy check, not a genuine rendered-viewport measurement, and correctly attributes the gap to the repo having no e2e/browser runner (`test_e2e: n/a`). No overstatement found — the disclosure does not claim pixel-perfect reflow verification anywhere.

Conclusion: TC-05 is reclassified **PASS** — the fix is genuine (non-tautological, empirically falsifiable) and the residual-gap disclosure is honest, matching the precedent set by AUTH-04's TC-02/TC-10/TC-14 partial-coverage disclosures.

### Task completion verification (MANDATORY)

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/src/services/userApi.js` (add `setAuthToken` export) | ✓ | ✓ (`export function setAuthToken(token)`) | PASS |
| T-02 | `frontend/src/services/userAuthService.js` (mocked `login`) | ✓ | ✓ (`export async function login(email, password)`, rejects generically, no `console.*` calls, mock comment present) | PASS |
| T-03 | `frontend/src/components/LoginForm.jsx` | ✓ | ✓ (fields, validation, `.form-group`/`.form-label`/`.form-input`/`.form-error`/`.spinner`/`.btn btn-primary` classes, `autoComplete`, `aria-required="true"` all present) | PASS |
| T-04 | `frontend/src/components/LoginScreen.jsx` | ✓ | ✓ (calls `login` → `setAuthToken` → `onLoginSuccess` in order; `sessionExpiredMessage` rendered separately from `.form-server-error`) | PASS |
| T-05 | `frontend/src/components/__tests__/LoginForm.test.jsx` | ✓ | ✓ (covers TC-01, TC-04, TC-05, TC-06, TC-07, TC-08, TC-09; TC-05 now non-tautological per independent verification above) | PASS |
| T-06 | `frontend/src/components/__tests__/LoginScreen.test.jsx` | ✓ | ✓ (covers TC-02, TC-03) | PASS |

All 6 PLAN tasks verified PASS. Round 1's caveat on T-05 (TC-05's assertion being defective) is resolved — the fix has been independently re-verified in this round, not merely re-asserted.

### Passed

| TC id         | Title                                                                                     |
|---------------|--------------------------------------------------------------------------------------------|
| AUTH-01-TC-01 | Login form renders email, password, and submit button with existing form classes           |
| AUTH-01-TC-02 | Submitting valid credentials shows loading state and calls onLoginSuccess with token        |
| AUTH-01-TC-03 | Submit button loading state clears immediately on auth call rejection                       |
| AUTH-01-TC-04 | Rejected mocked auth call shows single generic non-leaking error message                    |
| AUTH-01-TC-05 | Login form reflows without horizontal scroll at 320px viewport width (*caveat: structural proxy, see above*) |
| AUTH-01-TC-06 | Empty email and password fields block submit with field-level errors and no auth call       |
| AUTH-01-TC-07 | Password value never appears in DOM, console, or error message after a failed submit        |
| AUTH-01-TC-08 | Email/password inputs have labels, aria-required, aria-live=assertive error region (*caveat, unchanged from round 1*) |
| AUTH-01-TC-09 | Loading state (disabled button + spinner) appears within one render frame of click           |

### Regression coverage

| Regression TC | Guards against | Last attempt | Verdict |
|----------------|-----------------|--------------|---------|
| AUTH-01-TC-05 (round-1 fix, extended in place — no separate regression-id TC created; `attempts[]` history retained in JSON) | Round-1 tautological jsdom `scrollWidth`/`clientWidth` assertion silently always passing | PASS | PASS |

### Failed (bug blocks)

None.

### Manual follow-up

None — all 9 TCs are `automatable: true` and were executed.

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  9
- test_runner_runs:  3  (1x isolated `npx vitest run ... -t "TC-05"` mutation probe, 1x full `npm run test -- --run` [43/43], 1x `pytest -q` [16/16])
- stack_smoke_runs:  2  (react dev server start+curl 200; fastapi uvicorn start+curl 200 on /api/health)
- duration_seconds:  340
- timestamp:         2026-08-18T14:45:00Z
```
