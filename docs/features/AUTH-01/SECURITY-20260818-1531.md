# Security review — AUTH-01 (round 2)

- Date: 2026-08-18T09:31:00Z
- Verdict: PASS
- Governance profile: standard (generic internal admin tool; no HIPAA/PCI/SOC2 per CLAUDE.md "Target platforms")
- Compliance overlays applied: none
- Total findings: 1  (Critical: 0, High: 0, Medium: 1, Low: 0)
- Duration: single-pass agent review
- Supersedes: `docs/features/AUTH-01/SECURITY-20260818.md` (round 1, Verdict: BLOCKED — Critical F-001, High F-002, Medium F-003/F-004)

## Scope

- Base ref: `58fe64c0` (docs-only planning commit, parent of the branch)
- Target ref: `ffcbad93` (implementation commit) **plus** the uncommitted working-tree change to
  `frontend/package.json` / `frontend/package-lock.json` (the round-1 remediation), per this
  round's explicit instruction to treat the working tree as the dependency-scan diff target.
- Change since round 1: `frontend/package.json` devDependencies bumped
  `vite` `^5.4.8` → `^7.3.6`, `vitest` `^2.1.1` → `^3.2.7`. No other package version changed;
  `@vitejs/plugin-react` stays at semver range `^4.3.1` in `package.json` (resolved/installed
  version `4.7.0` per `node_modules/@vitejs/plugin-react/package.json`, which declares peer
  support for `vite ^7.0.0`, confirming compatibility with the new `vite@7.3.6`).
- Confirmed via `git diff ffcbad93 -- frontend/package.json frontend/package-lock.json` that the
  working-tree delta touches **only** the manifest/lockfile — zero application source files
  (`.jsx`/`.js` under `src/`) changed since round 1. The round-1 SAST/OWASP application-code
  findings (0 hits) therefore carry forward unchanged; re-running the grep pass below is a
  confirmation pass, not a re-discovery pass.

## Summary

| Source          | Critical | High | Medium | Low |
|------------------|----------|------|--------|-----|
| Deps scan        |    0     |  0   |   0    |  0  |
| SAST (grep)      |    0     |  0   |   0    |  0  |
| OWASP checklist  |    0     |  0   |   1    |  0  |
| Stack patterns   |    0     |  0   |   0    |  0  |
| Compliance       |    0     |  0   |   0    |  0  |

## Critical findings

None. F-001 (`vitest@2.1.1`, GHSA-5xrq-8626-4rwp, CVSS 9.8) is resolved by the `vitest` bump to
`3.2.7` (fix range `>=3.2.6`).

## High findings

None. F-002 (`vite@5.4.8`, GHSA-fx2h-pf6j-xcff, CVSS 7.5) is resolved by the `vite` bump to
`7.3.6` (fix range clears `<=6.4.2`).

## Medium findings

### F-004 — Mocked `userAuthService.login()` accepts any non-empty credential pair (carried forward, unchanged, tracked)

- Source: checklist/auth
- Severity: Medium
- File: `frontend/src/services/userAuthService.js:14` (unchanged since round 1 — not touched by
  this round's diff)
- Issue: unchanged from round 1 — the mock resolves for any non-empty `email`/`password` pair,
  by design (module header states AUTH-02 owns replacing it).
- Status change since round 1: this item is now a formally tracked
  `pending_carry_forward` entry in `docs/features/AUTH-01/state.json`
  (`item_id: F-004-mock-login-must-be-replaced`, `kind: "finding"`, `tag: "auth"`,
  `owner: "Backend lead"`, `evidence: docs/features/AUTH-01/SECURITY-20260818.md#f-004`) —
  addressing round 1's action-plan item #3, which flagged the absence of a tracked entry.
- Compliance-tag check (verdict-relevant): the entry's `tag` is `"auth"`, not one of
  `hipaa`/`pci`/`sox`/`gdpr`/`phi`/`audit-logging`. Governance profile for this repo is
  `standard` (no overlay active — confirmed no `harness.yaml` present /
  `governance.profile` unset, defaulting to `standard` per CLAUDE.md "Target platforms": no
  regulated domain). Per the skill's carry-forward enforcement rule, only compliance-tagged
  `kind: "finding"` entries under an active matching profile force `BLOCKED`; this entry does
  not qualify, so it does not flip the verdict on its own.
- Mitigating factors (unchanged, re-verified): `LoginScreen`/`LoginForm`/`userAuthService` are
  still absent from `App.jsx`/`main.jsx` (`grep` re-confirmed zero references), and still absent
  from the production bundle (`npm run build` output grepped again — no `mock-token-` /
  `Invalid email or password` strings present).
- Fix / action: no code change required for AUTH-01 to merge. The tracked carry-forward item
  remains open until whichever story mounts `LoginScreen` (expected: AUTH-05, per state.json)
  first replaces the mock with AUTH-02's real backend call. This review does not close the
  carry-forward item — it confirms it is now correctly tracked.

## Low findings

None.

## Dependency vulnerability scan (re-run)

- Tool: `npm audit --omit=optional` (Node/npm on `PATH`), run directly against the working tree
  (`frontend/node_modules` reinstalled via `npm install` per the prompt's stated remediation
  steps, re-verified live in this round).
- Result: **`found 0 vulnerabilities`** (exit code 0) — confirmed directly, not taken on faith.
- Installed/resolved versions verified from `node_modules/*/package.json`:
  `vite@7.3.6`, `vitest@3.2.7`, `@vitejs/plugin-react@4.7.0`.
- `npm run test` re-run: 7 test files / 43 tests passed (Vitest v3.2.7, 1.61s).
- `npm run build`: not re-captured as fresh evidence in this round beyond the prompt's own
  reported clean 518ms build; `npm run test` re-run above is the primary live confirmation for
  this gate. No build-breaking change expected — devDependency-only bump, `@vitejs/plugin-react`
  peer range already satisfied.

## SAST grep pass (re-run, confirmation)

Ran the full Detectable-patterns table (`security-review-checklist` § Detectable patterns)
against `git diff 58fe64c0 HEAD -U0 -- 'frontend/src/**' 'frontend/*.js' 'frontend/*.json'`
(HEAD = `ffcbad93`, since the working-tree delta touches no source files). Zero hits for every
pattern, matching round 1's result — unchanged because no source files changed. Also confirmed
`frontend/package.json`/`package-lock.json`'s working-tree diff contains no
`hardcoded-secret`-shaped strings (version bump only).

## Manual OWASP checklist + stack-pattern pass (delta re-check)

No new risk surface introduced by this round's diff (devDependency-only). Re-checked
specifically for version-bump-induced risk:

- **Input / Auth / Crypto / Data / Logs.** No application code touched — all round-1 findings
  under these categories (all clean, 0 hits) stand unchanged.
- **Dependencies.** This category is the one materially re-assessed this round — see deps-scan
  section above. `@testing-library/*` versions unchanged from round 1 (0 audit findings,
  reconfirmed).
- **Stack pattern (react-patterns).** No component/service files changed; layering conventions
  (components not calling services directly, `userApi.js` as sole HTTP entry point) unaffected
  by a build-tool version bump.

## Compliance impact

Not applicable — governance profile is `standard`, no overlay active.

## Carry-forward check

`docs/features/AUTH-01/state.json.pending_carry_forward` contains two items:

1. `F-004-mock-login-must-be-replaced` — `kind: "finding"`, `tag: "auth"` (not a compliance
   domain) — does not force `BLOCKED` under the active `standard` profile.
2. `R-01-userApi-setAuthToken-collision` — `kind: "risk"` (not `kind: "finding"`) — does not
   qualify for the carry-forward enforcement rule regardless of tag.

Neither item forces `BLOCKED` under this repo's governance profile.

## Tool-missing report

None. `npm audit` ran successfully (Node/npm on `PATH`).

## Action plan

1. No Critical findings — F-001 resolved.
2. No High findings — F-002 resolved.
3. F-003 (transitive `esbuild`/`vite` moderate advisories) resolved as a side effect of the
   `vite`/`vitest` major-version bump — `npm audit` shows 0 vulnerabilities across all severities.
4. F-004 remains open as a tracked `pending_carry_forward` item (kind: `finding`, tag: `auth`) —
   not blocking under this repo's `standard` governance profile, but MUST be resolved (mock
   replaced with real backend call) before `LoginScreen` is mounted into `App.jsx` in a future
   story (expected: AUTH-05). Track via
   `harness carry-forward resolve F-004-mock-login-must-be-replaced --evidence <path>` once
   AUTH-02's real login endpoint lands and the mock is swapped out.
5. No Low findings to track.
