# Validation report — AUTH-04

- Date: 2026-08-18T07:10:00Z
- Verdict: **PARTIAL**
- Mode: full
- Total: 15   Passed: 12   Partial: 2   Failed: 1   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~26 minutes (env setup + stack smoke + suites)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Integration  |  12   |  10  |   1  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| E2E (declared, run under vitest/jsdom) | 1 | 0 | 1 | 0 | 0 |

Notes:
- "Integration" row includes the 2 partial TCs (TC-02, TC-10) counted as Pass here (they pass against the observable proxy asserted by the actual test) but are called out separately below and drop the overall verdict floor to PARTIAL per the run-mode instruction, not per the PASS/FAIL/ERROR schema (there is no "PARTIAL" TC status in `last_run.verdict`; both are recorded as `verdict: "PARTIAL"` in the JSON per this run's explicit instruction, deviating from the strict PASS/FAIL/FLAKY enum in the schema to keep the AUTH-05 gap visible).
- TC-14 (declared `type: e2e`, executed under vitest/jsdom per PLAN.md § 7) is scored FAIL — see bug block below. This is a genuine implementation gap, not an environment issue.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/package.json` (add `jwt-decode` dependency) | ✓ | ✓ (`"jwt-decode": "^4.0.0"` present) | PASS |
| T-02 | `frontend/src/services/userApi.js` (auth-token exports + interceptor pair) | ✓ | ✓ (`getStoredToken`, `setAuthToken`, `clearAuthToken`, `registerUnauthorizedHandler`, request/response interceptors all present) | PASS |
| T-03 | `frontend/src/App.jsx` (auth state, mount expiry check, handler wiring) | ✓ | ✓ (`isAuthenticated`, `sessionExpiredMessage`, mount `useEffect` expiry check via `jwt-decode`, `registerUnauthorizedHandler` wiring, `handleLoginSuccess`/`handleLogout` all present) | PASS |
| T-04 | `frontend/src/services/__tests__/userApi.auth.test.js` | ✓ | ✓ (covers TC-04, TC-06, TC-07, TC-08, TC-09, TC-12, TC-13, TC-15 — all present and passing) | PASS |
| T-05 | `frontend/src/__tests__/App.auth.test.jsx` | ✓ | **PARTIAL — MISSING TC-14** (file covers TC-01, TC-02(partial), TC-03, TC-05, TC-10(partial), TC-13(mount path); PLAN.md § 7 Test Strategy table claims this file also covers TC-14, but no TC-14 test, `aria-live` query, or LoginScreen reference exists in the file) | **FAIL** |
| T-06 | `frontend/src/__tests__/App.auth.perf.test.jsx` | ✓ | ✓ (100-iteration mount benchmark, asserts `p95 < 100ms`, present and passing) | PASS |
| T-07 | `docs/config/project-commands.yaml` (append `jwt-decode` preflight smoke-check) | ✓ | ✓ (`test -d frontend/node_modules/jwt-decode` line present in `preflight:`) | PASS |

T-05's `Verdict: FAIL` (missing TC-14 coverage) drops the report floor to PARTIAL and is the primary cause of TC-14's TC-level FAIL below.

### Passed

| TC id            | Title                                                                 |
|------------------|------------------------------------------------------------------------|
| AUTH-04-TC-01    | Valid unexpired token restores session across refresh                  |
| AUTH-04-TC-03    | Expired stored token clears storage and sets session-expired message   |
| AUTH-04-TC-04    | 401 on mid-session API call clears token and redirects (interceptor)   |
| AUTH-04-TC-05    | Malformed/unparseable token treated as no token, not a crash           |
| AUTH-04-TC-06    | `localStorage.getItem` throwing is treated as no token                 |
| AUTH-04-TC-07    | `localStorage.setItem` throwing surfaces a readable error              |
| AUTH-04-TC-08    | Request interceptor attaches Authorization header only when present    |
| AUTH-04-TC-09    | Unauthorized handler dedup guard — does not double-fire                |
| AUTH-04-TC-11    | Auth-state check on mount completes within 100ms budget (performance)  |
| AUTH-04-TC-12    | No password/hash ever written to localStorage (security)               |
| AUTH-04-TC-13    | Session-expiry event logged with no PII, both interceptor and mount path |
| AUTH-04-TC-15    | Login token extraction matches `{ token }` contract shape              |

### Partial — full behavior pending AUTH-05

Per `docs/features/AUTH-04/FLAGS.md` AF-04 (triaged: defer). App.jsx does not yet gate rendering on `isAuthenticated` — that conditional Login-vs-protected-view switch is explicitly AUTH-05's scope. Both TCs below pass against the observable proxies that exist today; the literal AC wording cannot be fully exercised until AUTH-05 lands.

#### AUTH-04-TC-02 — No stored token renders Login and issues zero protected data-fetch calls — PARTIAL

- Proxy asserted today: no token present → `clearAuthToken` is not called and no `auth_session_expired` log fires (nothing to expire). Test: `frontend/src/__tests__/App.auth.test.jsx::TC-02`. PASSES.
- Not yet exercisable: "Login is rendered within 100ms" and "no GET /api/dashboard/stats or GET /api/users request is issued" — today `App.jsx` unconditionally mounts `Dashboard`, which *does* call `getDashboardStats`/`getUsers` regardless of `isAuthenticated`, because the conditional render gate is AUTH-05 scope.
- Action: re-run TC-02 in full once AUTH-05 adds the Login/protected-view conditional render.

#### AUTH-04-TC-10 — sessionExpiredMessage threaded to LoginScreen as a distinct prop — PARTIAL

- Proxy asserted today: `registerUnauthorizedHandler` is called exactly once on mount, and invoking the registered handler does not throw. Test: `frontend/src/__tests__/App.auth.test.jsx::TC-10 (partial)`. PASSES.
- Not yet exercisable: `LoginScreen` does not exist / is not rendered by `App.jsx` in this story, so the actual prop-threading (`sessionExpiredMessage` prop present, no `authError` prop) cannot be asserted.
- Action: re-run TC-10 in full once AUTH-05 mounts `LoginScreen` and threads the prop.

### Failed (bug blocks)

#### AUTH-04-TC-14 — sessionExpiredMessage renders in an aria-live region on the Login screen — FAIL (Priority: Should)

- AC: given `sessionExpiredMessage` is set to a non-null string and `LoginScreen` is rendered, then the message text appears inside an `aria-live` region reachable by assistive technology.
- Attempts: 1 (no retries triggered — this is a missing-implementation finding, not a flake)
- Steps executed:
  1. `grep -rn "aria-live" frontend/src` → only match is an unrelated `<span aria-live="polite">` in `frontend/src/pages/Users.jsx`, not related to auth/session-expiry.
  2. `grep -rn "LoginScreen" frontend/src` → no component named `LoginScreen` exists anywhere in the frontend source tree on this branch.
  3. Read `frontend/src/__tests__/App.auth.test.jsx` in full (132 lines) — contains TC-01, TC-02, TC-03, TC-05, TC-10 only; no `it(...)` block references TC-14 or queries for an `aria-live` element.
- Expected: a passing automated TC-14 test asserting an `aria-live` region contains the session-expired message.
- Actual: no test exists; no `aria-live` region exists for this purpose; `LoginScreen` itself does not exist yet.
- Failure reason: this is the same underlying AUTH-05 scope boundary that produces TC-02/TC-10's partial status (Login rendering is out of scope for AUTH-04), **but unlike TC-02/TC-10 it is not disclosed in `docs/features/AUTH-04/FLAGS.md` AF-04**, and PLAN.md § 7's Test Strategy table asserts (incorrectly) that `App.auth.test.jsx` covers TC-14. This is a documentation/plan-vs-implementation discrepancy, not merely a deferred scope item.
- Likely cause: T-05 (`App.auth.test.jsx`) was scoped in PLAN.md to include TC-14 but the implementation did not add it, and FLAGS.md AF-04 was not updated to disclose the same AUTH-05 dependency for TC-14 that it already discloses for TC-02/TC-10.
- Recommended action: either (a) add a TC-14 test asserting an `aria-live`-equivalent observable proxy consistent with the TC-02/TC-10 pattern and update FLAGS.md AF-04 to list TC-14 alongside TC-02/TC-10, or (b) correct PLAN.md § 7's Test Strategy table to remove the false claim of TC-14 coverage until AUTH-05 lands. Does not block AUTH-04 merge on its own priority (`Should`) but should not be silently left unflagged.

### Manual follow-up

None — all 15 TCs are `automatable: true`.

### Environmental note (not a defect)

- TC-04 and TC-09 (401 handling) were validated against a **mocked axios interceptor error object** (`{ response: { status: 401 } }`), not a live backend 401. Verified by direct probe: this branch's backend (`feature/AUTH-04`, branched from `feature/AUTH-01`) has no auth-guarded endpoint wired into `app/main.py` — `curl -H "Authorization: Bearer not-a-real-token" http://127.0.0.1:8000/api/users` returns `200`, and `/api/auth/login` returns `404`. Leftover `.pyc` files reference `auth.py`/`auth_service.py`, but the corresponding `.py` sources are not present/wired on this branch (AUTH-02/AUTH-03 not yet merged here). This is expected given the story's frontend-only, cross-story sequencing and does not indicate an AUTH-04 defect — the interceptor code itself is exercised for real, just with a simulated error object rather than a live HTTP round-trip. Re-validate TC-04/TC-09 against a live 401 once AUTH-02/AUTH-03 are merged into this branch's history.
- Pre-existing Docker containers `crud-frontend`/`crud-backend` (built ~22h prior, stale relative to this branch's uncommitted changes) were squatting ports 5173/8000 at validation start. Per `validation-execution` § Preflight port-free check, these were stopped (`docker compose down`) rather than silently reused, and the real dev-mode servers (`npm run dev`, `uvicorn --reload`) were started fresh for stack smoke.

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  17
- test_runner_runs:  3  (1x `npm run test -- --run` [47 tests, 8 files], 1x `pytest -q` [16 tests], 1x standalone node jwt-decode probe)
- stack_smoke_runs:  2  (backend uvicorn start+healthcheck, frontend vite start+healthcheck; preceded by `docker compose down` to clear a port squatter)
- duration_seconds:  ~1560
- timestamp:         2026-08-18T07:10:00Z
```
