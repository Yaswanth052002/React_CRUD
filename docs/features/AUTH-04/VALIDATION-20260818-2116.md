# Validation report — AUTH-04

- Date: 2026-08-18T15:46:00Z
- Verdict: PARTIAL
- Mode: rerun (post-merge full-epic regression re-validation on feature/AUTH-01, HEAD 6ab0e53e)
- Total: 15   Passed: 12   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0   (3 carried-forward PARTIAL — disclosed, pre-existing scope limitation, not a new regression)
- Duration: ~9 minutes wall-clock (preflight + stack smoke + test + build)

Scope note: this is a regression re-validation of story AUTH-04 specifically, now that all 8 AUTH stories are merged onto the shared `feature/AUTH` integration branch at commit 6ab0e53e (confirmed via `git branch --show-current`). Backend pytest was intentionally skipped per instruction (frontend-only story, sibling AUTH-03 re-validation task has an uncommitted, in-scope-excluded edit to `backend/app/services/user_service.py` and `backend/tests/test_users.py` — those files were not read, opened, or modified by this run; `git status --short` re-confirmed after this run shows those two files as the only backend modifications, unrelated to this validation's writes).

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Integration  |  13   |  10  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |

(3 of the 13 Integration TCs — TC-02, TC-10, TC-14 — carry a `PARTIAL` verdict; their single attempt this round still returned `PASS` on the disclosed proxy assertion. They are counted under "Pass" in the Summary table's Pass column per the raw attempt outcome, and separately called out below since a `PARTIAL`-verdict TC caps the overall report verdict at `PARTIAL` per the verdict rule.)

Stack smoke: both stacks (`react`, `fastapi`) started and answered their health checks with the whole epic merged:
- `fastapi`: `JWT_SECRET_KEY` exported before start (AUTH-08's fail-fast startup guard) → `uvicorn app.main:app --port 8000` booted clean, `GET http://127.0.0.1:8000/api/health` → `200 {"status":"ok"}`.
- `react`: `npm run dev -- --port 5173` booted clean (Vite bound to IPv6 `localhost`, not `127.0.0.1` — confirmed reachable via `http://localhost:5173/`, returned the dashboard shell HTML, `200`).

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/package.json` (add `jwt-decode` dependency) | ✓ | ✓ (`"jwt-decode": "^4.0.0"` present) | PASS |
| T-02 | `frontend/src/services/userApi.js` (interceptor pair + token exports) | ✓ | ✓ (`getStoredToken`, `setAuthToken`, `clearAuthToken`, `registerUnauthorizedHandler` all present, unchanged signatures) | PASS |
| T-03 | `frontend/src/App.jsx` (auth state, mount-time expiry check, handler registration) | ✓ | ✓ (`isAuthenticated`, `sessionExpiredMessage`, `registerUnauthorizedHandler` wiring, `handleLoginSuccess`, `handleLogout` all present) | PASS |
| T-04 | `frontend/src/services/__tests__/userApi.auth.test.js` | ✓ | ✓ (TC-04,06,07,08,09,12,13,15 all present as named tests, all 8 pass) | PASS |
| T-05 | `frontend/src/__tests__/App.auth.test.jsx` | ✓ | ✓ (TC-01,02,03,05,10,14 all present as named tests, all 7 pass) | PASS |
| T-06 | `frontend/src/__tests__/App.auth.perf.test.jsx` | ✓ | ✓ (TC-11 present, p95 < 100ms assertion passes) | PASS |
| T-07 | `docs/config/project-commands.yaml` (jwt-decode preflight smoke-check) | ✓ | ✓ (`test -d frontend/node_modules/jwt-decode` present in `preflight:`) | PASS |

All 7 PLAN.md task rows verified intact post-merge. No task regressed.

### Passed

| TC id            | Title                                  |
|------------------|-----------------------------------------|
| AUTH-04-TC-01 | Valid unexpired token restores session across refresh without showing Login |
| AUTH-04-TC-03 | Expired stored token clears storage and redirects to Login with session-expired message |
| AUTH-04-TC-04 | 401 on a mid-session API call clears token and redirects to Login |
| AUTH-04-TC-05 | Malformed/unparseable token is treated as no token, not a crash |
| AUTH-04-TC-06 | localStorage.getItem throwing is treated as no token |
| AUTH-04-TC-07 | localStorage.setItem throwing surfaces a readable error |
| AUTH-04-TC-08 | Request interceptor attaches Authorization header only when a token is present |
| AUTH-04-TC-09 | Unauthorized handler does not fire twice on double in-flight 401 |
| AUTH-04-TC-11 | Auth-state check on mount completes within the 100ms budget |
| AUTH-04-TC-12 | No password or password hash is ever written to localStorage |
| AUTH-04-TC-13 | Session-expiry event is logged with no PII |
| AUTH-04-TC-15 | Login token extraction matches AUTH-02's `{ token: string }` response shape |

### Carried-forward PARTIAL (disclosed, pre-existing — not a new regression)

These three TCs pass their disclosed proxy assertion this round, same as every prior round. They were re-confirmed, not newly discovered:

- **AUTH-04-TC-02** — No stored token renders Login and issues zero protected data-fetch calls. Proxy assertion (no session-expired event fires when there's nothing to expire) passes. AUTH-04's own scope boundary (App.jsx render-gating) was completed by AUTH-05, which is merged on this branch — see coverage note below.
- **AUTH-04-TC-10** — sessionExpiredMessage threaded to LoginScreen as a named prop, never `authError`. AUTH-04's own proxy (handler registers, runs without throwing) passes. Full DOM-level prop-threading is now independently covered by AUTH-05's own test suite (`App.authGate.test.jsx` TC-07/TC-08), which also passes on this branch (see Coverage note below) — this closes the practical gap even though AUTH-04's own TC entry remains labeled PARTIAL per its original scope boundary.
- **AUTH-04-TC-14** — sessionExpiredMessage renders in an aria-live region on the Login screen. AUTH-04's own state-value proxy passes. `LoginScreen.jsx` (AUTH-01/05 scope, now merged) does implement `role="alert" aria-live="assertive"` around `sessionExpiredMessage` — confirmed by inspection — but no explicit `aria-live` attribute assertion exists in the currently-run test suite (`LoginScreen.test.jsx` only asserts the message renders "in its own region distinct from a submit-attempt error", not the `aria-live` attribute itself). This is a coverage gap in AUTH-01/05's own test suite, not an AUTH-04 regression — flagged for the team's awareness, not actioned here (outside AUTH-04's scope per `surgical-changes`).

**Verdict impact**: per the verdict rule, any TC with `verdict: PARTIAL` (used interchangeably with a documented, disclosed FLAKY-adjacent limitation here) caps the report at `PARTIAL` — none of these three regressed (all attempts remain PASS-on-proxy, unchanged since the last round), so this is a **carry-forward PARTIAL**, not a new failure.

### Coverage note (informational — no action taken, outside AUTH-04 scope)

Now that the whole epic is merged, the surrounding growth confirms AUTH-04's forward-references were consumed correctly by sibling stories, with no drift in AUTH-04's own contract:

- AUTH-05's `App.authGate.test.jsx` (11 tests, all passing) independently re-verifies `sessionExpiredMessage` reaches `LoginScreen` verbatim (its own TC-07) and that `LoginScreen` never receives an `authError` prop (its own TC-08) — both consistent with AUTH-04's PLAN.md § 3 module-hierarchy contract for `handleLoginSuccess`/`sessionExpiredMessage`.
- AUTH-07's `userApi.logout.test.js` (10 tests, all passing) confirms `logout()` calls `clearAuthToken()` exactly once, with no residual auth artifacts — consistent with AUTH-04's `clearAuthToken` contract being called, not reimplemented.
- `LoginScreen.test.jsx` (3 tests, all passing) confirms `onLoginSuccess`/`setAuthToken` call ordering matches AUTH-04's ADR-1 locked sequence (`userApi.login` → `userApi.setAuthToken` → `onLoginSuccess`).

No sibling story edit touched `getStoredToken`/`setAuthToken`/`clearAuthToken`/`registerUnauthorizedHandler`'s implementation in `userApi.js`, nor `isAuthenticated`/`sessionExpiredMessage`/`handleLoginSuccess`/`handleLogout` in `App.jsx` — grep-confirmed unchanged signatures, all 15 AUTH-04 TCs still pass against the merged codebase.

### Flaky tests

None observed this round (no attempt required a retry).

### Performance budget breaches

None. AUTH-04-TC-11: target `p95 < 100ms across 100 iterations`, measured assertion passed (perf test completed in 767ms wall time for the full 100-iteration loop; exact per-iteration p95 not machine-parsed from vitest stdout, consistent with prior rounds' recording convention).

### Regression coverage

No TC in `docs/test-cases/AUTH-04.json` carries a `regression-<id>` tag. AUTH-04-TC-09 is tagged `regression-guard` (a guard-designed TC from initial authoring, not a fix-loop-generated regression tag) — it passed this round, same as every prior round.

### Failed (bug blocks)

None. Zero FAIL or ERROR outcomes this round.

### Errored

None.

### Manual follow-up

None. All 15 AUTH-04 TCs are `automatable: true`; no manual TCs are declared for this story.

### Budget-unparseable NFRs

None flagged. AUTH-04-NFR-performance's budget (`p95 < 100ms across 100 iterations`) parsed and enforced; `budget_pass: true`.

### Independent frontend suite (full regression, whole epic merged)

- `cd frontend && npm run test -- --run`: **93/93 tests passed** across 14 test files (includes all 8 AUTH stories' frontend test suites — `App.auth.test.jsx`, `App.auth.perf.test.jsx`, `userApi.auth.test.js`, `App.authGate.test.jsx`, `LoginScreen.test.jsx`, `LoginForm.test.jsx`, `userApi.logout.test.js`, `App.logoutFocus.test.jsx`, `App.routing.test.jsx`, `Settings.test.jsx`, `Users.search.test.jsx`, `Users.crud.test.jsx`, `Dashboard.search.test.jsx`, `userApi.test.js`). Zero failures.
- `cd frontend && npm run build`: **succeeded** — `vite build` transformed 98 modules, emitted `dist/index.html`, `dist/assets/index-*.css` (17.70 kB), `dist/assets/index-*.js` (232.28 kB), no errors or warnings.
- Backend `pytest` intentionally **skipped** per task instruction (frontend-only story; sibling AUTH-03 re-validation task has an uncommitted, out-of-scope edit in flight on `backend/app/services/user_service.py` / `backend/tests/test_users.py` — neither file was read or modified by this validation run).

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  17
- test_runner_runs:  2  (npm run test -- --run [93 tests, vitest]; npm run build [vite])
- stack_smoke_runs:  2  (fastapi: uvicorn start + /api/health poll; react: vite dev start + / poll)
- duration_seconds:  ~540
- timestamp:         2026-08-18T15:46:00Z
```
