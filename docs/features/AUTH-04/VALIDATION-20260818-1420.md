# Validation report — AUTH-04 (round 2 — post fix-loop)

- Date: 2026-08-18T14:20:00Z
- Verdict: **PARTIAL**
- Mode: full (re-run of all 15 TCs + preflight + stack smoke; supersedes VALIDATION-20260818-0710.md)
- Total: 15   Passed: 12   Partial: 3   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~14 minutes (env setup + stack smoke + suites)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Integration  |  12   |  10  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| E2E (declared, run under vitest/jsdom) | 1 | 0 | 0 | 0 | 0 |

Notes:
- "Integration" row's 10/12 pass count treats TC-02 and TC-10 as passing against their disclosed observable proxies (both `verdict: PARTIAL` in the JSON, consistent with round 1). TC-14 is counted in the "E2E" row and also now scored `verdict: PARTIAL` (not FAIL) — see re-assessment below. No TC scored FAIL or ERROR this round.
- Round-1's single FAIL (TC-14) is resolved as a **re-classification**, not a code fix: the underlying capability gap (no `LoginScreen`/aria-live region until AUTH-05) is unchanged and was never expected to close in this story. What changed is that the *disclosure and documentation defect* — the actual round-1 bug — is now fixed.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/package.json` (add `jwt-decode` dependency) | ✓ | ✓ (`"jwt-decode": "^4.0.0"` present, line 14) | PASS |
| T-02 | `frontend/src/services/userApi.js` (auth-token exports + interceptor pair) | ✓ | ✓ (`getStoredToken`, `setAuthToken`, `clearAuthToken`, `registerUnauthorizedHandler` all present) | PASS |
| T-03 | `frontend/src/App.jsx` (auth state, mount expiry check, handler wiring) | ✓ | ✓ (`isAuthenticated`, `sessionExpiredMessage`, `registerUnauthorizedHandler` wiring present) | PASS |
| T-04 | `frontend/src/services/__tests__/userApi.auth.test.js` | ✓ | ✓ (8 tests covering TC-04/06/07/08/09/12/13/15, all passing) | PASS |
| T-05 | `frontend/src/__tests__/App.auth.test.jsx` | ✓ | ✓ (**re-verified this round**: file now contains 7 tests — TC-01, TC-02, TC-03, TC-05, TC-10 (partial), and two TC-14 (partial) tests asserting `sessionExpiredMessage` is set to the exact string "Your session has expired. Please log in again." when the unauthorized handler fires, and never set otherwise; both TC-14 tests read in full and confirmed genuine, non-tautological assertions against `capturedStateSets`) | PASS |
| T-06 | `frontend/src/__tests__/App.auth.perf.test.jsx` | ✓ | ✓ (100-iteration mount benchmark, asserts `p95 < 100ms`, present and passing) | PASS |
| T-07 | `docs/config/project-commands.yaml` (append `jwt-decode` preflight smoke-check) | ✓ | ✓ (`test -d frontend/node_modules/jwt-decode` line present in `preflight:`) | PASS |

T-05's round-1 `FAIL` (missing TC-14 coverage) is now `PASS` — the file contains genuine TC-14 proxy tests, not a tautological or vacuous assertion. All 7 PLAN tasks are PASS this round.

### Passed

| TC id            | Title                                                                 |
|------------------|------------------------------------------------------------------------|
| AUTH-04-TC-01    | Valid unexpired token restores session across refresh                  |
| AUTH-04-TC-03    | Expired stored token clears storage and sets session-expired message   |
| AUTH-04-TC-04    | 401 on mid-session API call clears token and redirects (interceptor)   |
| AUTH-04-TC-05    | Malformed/unparseable token treated as no token, not a crash           |
| AUTH-04-TC-06    | `localStorage.getItem` throwing is treated as no token                 |
| AUTH-04-TC-07    | `localStorage.setItem` throwing surfaces a readable error              |
| AUTH-04-TC-08    | Request interceptor attaches Authorization header only when present    |
| AUTH-04-TC-09    | Unauthorized handler dedup guard — does not double-fire                |
| AUTH-04-TC-11    | Auth-state check on mount completes within 100ms budget (performance)  |
| AUTH-04-TC-12    | No password/hash ever written to localStorage (security)               |
| AUTH-04-TC-13    | Session-expiry event logged with no PII, both interceptor and mount path |
| AUTH-04-TC-15    | Login token extraction matches `{ token }` contract shape              |

### Partial — full behavior pending AUTH-05

Per `docs/features/AUTH-04/FLAGS.md` AF-04 (triaged: defer; corrected 2026-08-18). App.jsx does not yet gate rendering on `isAuthenticated` and `LoginScreen` does not yet exist — both are explicitly AUTH-05's scope. All three TCs below pass against the observable proxies that exist today; the literal AC wording cannot be fully exercised until AUTH-05 lands.

#### AUTH-04-TC-02 — No stored token renders Login and issues zero protected data-fetch calls — PARTIAL

- Unchanged from round 1. Proxy asserted: no token present → `clearAuthToken` is not called and no `auth_session_expired` log fires. Test: `frontend/src/__tests__/App.auth.test.jsx::TC-02`. PASSES.
- Not yet exercisable: "Login rendered within 100ms" / "zero protected fetch calls" — `App.jsx` unconditionally mounts `Dashboard` today (AUTH-05 scope).

#### AUTH-04-TC-10 — sessionExpiredMessage threaded to LoginScreen as a distinct prop — PARTIAL

- Unchanged from round 1. Proxy asserted: `registerUnauthorizedHandler` is called exactly once on mount and invoking it does not throw. Test: `frontend/src/__tests__/App.auth.test.jsx::TC-10 (partial)`. PASSES.
- Not yet exercisable: `LoginScreen` does not exist / is not rendered, so prop-threading cannot be asserted.

#### AUTH-04-TC-14 — sessionExpiredMessage renders in an aria-live region on the Login screen — PARTIAL (re-classified from FAIL; Priority: Should)

- AC: given `sessionExpiredMessage` is set to a non-null string and `LoginScreen` is rendered, then the message text appears inside an `aria-live` region reachable by assistive technology.
- Round-1 finding (FAIL): the AC gap itself was expected/scope-bound (same class as TC-02/TC-10), but it was **undisclosed** in FLAGS.md AF-04 and PLAN.md § 7 **falsely claimed** `App.auth.test.jsx` already covered TC-14. That combination — undisclosed gap + false documentation — was the actual defect, not the mere absence of `LoginScreen`.
- Round-2 re-assessment (this run):
  1. `docs/features/AUTH-04/FLAGS.md` AF-04 re-read in full: TC-14 is now explicitly named alongside TC-02/TC-10 in the main disclosure paragraph, plus a dated "Correction (2026-08-18, round-1 fix ...)" addendum documenting exactly what was wrong and what changed. Disclosure gap: **closed**.
  2. `docs/features/AUTH-04/PLAN.md` § 7 Test Strategy table, TC-14 row, re-read: no longer claims full coverage; explicitly states "this row previously claimed full TC-14 coverage; it does not exist yet" and describes the proxy test added. § 5 T-05 Notes column independently corroborates the same "partial/proxy only, NOT full passes" language for TC-14. False-claim gap: **closed**, and consistent across both sections.
  3. `frontend/src/__tests__/App.auth.test.jsx` re-read in full (194 lines): two new `it(...)` blocks exist — "TC-14 (partial): sessionExpiredMessage state is set to the exact required string when the unauthorized handler fires" and its negative counterpart. Both wrap React's `useState` (via `vi.hoisted` + `vi.mock("react", ...)`) to capture every value passed to every setter, then assert `capturedStateSets` does/does not contain the exact string `"Your session has expired. Please log in again."`. This is a genuine, non-tautological assertion on the literal state value AUTH-04 owns (not a placeholder `expect(true).toBe(true)`), and both tests were executed and PASS under `npm run test -- --run` (confirmed in the 49-test run below).
  4. `grep -rn "aria-live" frontend/src` and `grep -rn "LoginScreen" frontend/src` re-run: same result as round 1 — no aria-live region or `LoginScreen` component exists. This is expected and disclosed, not a new finding.
- Verdict: **PARTIAL** (not FAIL). The literal AC (aria-live DOM region) genuinely cannot be exercised until AUTH-05 lands `LoginScreen` — that was never going to change within AUTH-04's scope. What round 1 correctly flagged as a blocking defect was the *undisclosed gap + false PLAN.md claim*, and both are now fixed. TC-14 is therefore held to the identical bar as TC-02/TC-10: PARTIAL, proxy-verified, re-run pending AUTH-05.
- Action: re-run TC-14 in full once AUTH-05 mounts `LoginScreen` and adds its aria-live region.

### Failed (bug blocks)

None this round.

### Manual follow-up

None — all 15 TCs are `automatable: true`.

### Environmental note (not a defect)

- TC-04 and TC-09 (401 handling) remain validated against a **mocked axios interceptor error object**, not a live backend 401 — unchanged from round 1; this branch (`feature/AUTH-04`) still has no auth-guarded endpoint wired (AUTH-02/AUTH-03 not merged here). Not re-probed this round since no backend code changed; carried forward from round 1's finding.
- Frontend stack-smoke healthcheck: Vite bound to `localhost` on an IPv6-only socket (`TCP localhost:5173 (LISTEN)`, IPv6), so `curl http://127.0.0.1:5173/` returned connection-refused while `curl http://localhost:5173/` returned `200`. This is the documented IPv4/IPv6 mismatch false-FAIL source called out in the `validation-execution` skill's preflight guidance — not an AUTH-04 defect. Backend healthcheck (`http://127.0.0.1:8000/api/health`) returned `200` directly on IPv4 with no issue.
- No production code changed in this round — only `docs/features/AUTH-04/FLAGS.md`, `docs/features/AUTH-04/PLAN.md`, and `frontend/src/__tests__/App.auth.test.jsx` (test file) per the fix-loop scope. Backend suite re-run to confirm it remains unaffected: 16/16 pass, unchanged from round 1.

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  13
- test_runner_runs:  2  (1x `npm run test -- --run` [49 tests, 8 files, all pass], 1x `pytest -q` [16 tests, all pass])
- stack_smoke_runs:  2  (backend uvicorn start+healthcheck on 127.0.0.1:8000, frontend vite start+healthcheck on localhost:5173; both torn down via pkill after verdict recorded)
- duration_seconds:  ~840
- timestamp:         2026-08-18T14:20:00Z
```
