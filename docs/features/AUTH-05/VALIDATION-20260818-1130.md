# Validation report — AUTH-05

- Date: 2026-08-18T11:30:00Z
- Verdict: PASS
- Mode: full (fresh run — prior interrupted invocation's artifacts not trusted or reused)
- Total: 11   Passed: 11   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~34 minutes wall-clock (preflight + stack smoke + full suite runs + report)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   0   |   0  |   0  |   0   |   0   |
| Integration  |   9   |   9  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| E2E (declared; executed under Vitest/jsdom per PLAN §7 precedent) | (rolled into Integration count above, TC-11) | | | | |

Note: AUTH-05's 11 TCs are typed `integration` (TC-01..07), `contract` (TC-08), `performance` (TC-09), `security` (TC-10), and declared-`e2e`-executed-as-integration (TC-11) — all live in one Vitest file, `frontend/src/__tests__/App.authGate.test.jsx`, per PLAN.md §7's documented rationale (no browser-automation/perf runner configured in this repo; same precedent as AUTH-01/AUTH-04). Broken out by TC type above for traceability; row totals sum to 11/11 pass.

Full repo test suites (not just the 11 AUTH-05 TCs) were also independently re-run for regression assurance:
- Backend: `pytest` → 47 passed, 1 deselected (perf smoke test excluded by default marker config, consistent with implementation-agent's report)
- Frontend: `npm run test` (vitest run) → 11 test files, 70 tests, all passed
- Frontend build (`npm run build`): clean, no errors, no new warnings

### Stack smoke

| Stack   | Command                                              | Check                                    | Result |
|---------|-------------------------------------------------------|-------------------------------------------|--------|
| fastapi | `cd backend && uvicorn app.main:app --port 8000`      | `GET http://127.0.0.1:8000/api/health` → `{"status":"ok"}` | PASS |
| react   | `cd frontend && npm run dev -- --port 5173`           | `GET http://localhost:5173/` → 200, dashboard HTML served | PASS |

Note: Vite v7's dev server bound to `::1` (IPv6 loopback) only, not `127.0.0.1` (IPv4). A `curl 127.0.0.1:5173` returned connection-refused (exit 7) while `curl localhost:5173` (which resolves to `::1` on this host) returned 200. This is the known IPv4/IPv6 mismatch false-FAIL source called out in the `validation-execution` skill's preflight guidance. Not a code defect — recorded here as an environment observation for future runs; consider adding `--host 127.0.0.1` to the `Run:` command in `docs/config/stack-smoke.md` react section to make the check deterministic.

### Task completion verification (MANDATORY)

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|----------------|---------|
| T-01 | `frontend/src/App.jsx` | ✓ | ✓ — `isAuthenticated`-gated conditional render added; renders exactly `<LoginScreen onLoginSuccess={handleLoginSuccess} sessionExpiredMessage={sessionExpiredMessage} />` when false; unchanged Sidebar+Header+activeView tree when true; `handleLoginSuccess` now also calls `setActiveView("dashboard")` (AUTH-05-FR-2/C-5); inline contract comment present at the conditional (AUTH-05-FR-1..3/C-1/C-2/C-4) | PASS |
| T-02 | `frontend/src/pages/__tests__/App.routing.test.jsx` | ✓ | ✓ — `getStoredToken` mock changed from `vi.fn()` to `vi.fn(() => MOCK_TOKEN)` with a real base64url header.payload.signature triple (far-future `exp`); `registerUnauthorizedHandler` stub already present and unchanged; pre-existing USR-01 nav/loading/a11y assertions verified passing under the new auth gate (3/3 tests pass) | PASS |
| T-03 | `frontend/src/__tests__/App.authGate.test.jsx` | ✓ | ✓ — new file exists, covers AUTH-05-TC-01 through TC-11 exactly (one `it()` block per TC, verified by independent re-run: 11/11 pass in isolation) | PASS |

All 3/3 PLAN tasks verified PASS. `App.jsx` diff was read directly and cross-checked line-by-line against PLAN.md §2/§3/§5 — no drift found (import of `LoginScreen` added, conditional render branch matches the documented shape exactly, `handleLoginSuccess` extended exactly as specified, no `authError` prop anywhere, no new state introduced).

### Passed

| TC id         | Title                                                                          |
|---------------|----------------------------------------------------------------------------------|
| AUTH-05-TC-01 | Unauthenticated visitor sees Login-only view, no Sidebar/Header mounted          |
| AUTH-05-TC-02 | activeView cannot be set to a protected view while unauthenticated              |
| AUTH-05-TC-03 | Authenticated user sees Sidebar with exactly three working nav entries          |
| AUTH-05-TC-04 | Successful login transitions directly to Dashboard without reload              |
| AUTH-05-TC-05 | Post-login activeView resets to dashboard even if a prior session left it on users |
| AUTH-05-TC-06 | Auth state becoming invalid falls back to Login-only, unmounting stale view     |
| AUTH-05-TC-07 | sessionExpiredMessage is threaded through to LoginScreen unchanged              |
| AUTH-05-TC-08 | No authError prop is ever passed to LoginScreen                                |
| AUTH-05-TC-09 | Auth-gate branch adds no additional network round trip on login/logout toggle  |
| AUTH-05-TC-10 | Protected components do not mount (not merely CSS-hide) when unauthenticated   |
| AUTH-05-TC-11 | Sidebar nav-item buttons remain keyboard-reachable with visible focus          |

### Flaky tests

None.

### Performance budget breaches

None. AUTH-05-TC-09 (NFR-performance): target is "no additional network request beyond AUTH-04's existing auth-state check; branch resolves within same paint" — this is a call-count comparison (0 additional `userApi.getUsers`/`getDashboardStats` calls observed across a false→true→false toggle), not a latency measurement, per PLAN.md §7's documented rationale. `budget_pass: true`.

### Regression coverage

No `regression-<id>`-tagged TCs exist in `docs/test-cases/AUTH-05.json` (this is the first validation pass for this story; no prior fix-loop). Regression assurance for the pre-existing `App.routing.test.jsx` (USR-01) was verified separately under Task completion verification / T-02 above — all 3 of its tests pass under the new auth gate.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None. All 11 TCs are `automatable: true`; none flagged manual.

### Observations (not verdict-affecting)

- TC-11's focus-visible assertion (`button.style.outline === ""`) checks that no *inline* style suppresses the CSS `:focus-visible` rule, run under jsdom (no real paint/layout engine) — it does not independently confirm the global stylesheet's `:focus-visible` rule actually produces a rendered visible outline in a real browser. This matches PLAN.md §7's explicitly documented and plan-validated rationale (no browser-automation runner configured in this repo, same precedent as AUTH-01/AUTH-04's own declared-`e2e` TCs), so it is not scored as a failure, but it is weaker evidence than a real Playwright `:focus-visible` computed-style check would provide. Flagging for awareness only — no PLAN task requires resolving this within AUTH-05's scope.
- Vite dev-server IPv6-only binding (noted under Stack smoke above) — environment observation, not a code defect.

### Proof of run

- session_id: 64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations: 18
- test_runner_runs: 4 (backend `pytest` full suite; frontend `npm run test` full suite; frontend `npx vitest run App.authGate.test.jsx` isolated re-run; frontend `npm run build`)
- stack_smoke_runs: 2 (fastapi start+healthcheck; react dev-server start+HTTP check)
- duration_seconds: ~2040
- timestamp: 2026-08-18T11:30:00Z
