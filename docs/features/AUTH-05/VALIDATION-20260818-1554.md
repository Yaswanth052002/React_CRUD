# Validation report — AUTH-05

**Post-epic-merge full regression re-validation.** Branch `feature/AUTH` (the single shared
integration branch for all 8 AUTH stories), HEAD `eb946e72` ("fix(auth): a freshly created user
can never log in (must_reset_password bug)"). This supersedes
`docs/features/AUTH-05/VALIDATION-20260818-1730.md` (round 2, PASS, pre-epic-merge / post-security-
fix only). This round confirms AUTH-05's `isAuthenticated`-gated render branch and
`handleLoginSuccess`'s `setActiveView("dashboard")` reset are still correct now that AUTH-06
(Settings screen) and AUTH-07 (logout) have landed around it, and that the just-merged
`must_reset_password` backend fix does not regress AUTH-05's UI wiring.

- Date: 2026-08-18T15:54:39Z
- Verdict: **PASS**
- Mode: rerun (`--rerun` semantics — reused existing flow files, no TC generation)
- Total: 11   Passed: 11   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~11 minutes (preflight + stack smoke + live login e2e smoke + full frontend/backend
  suites + build)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Integration  |   7   |   7  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| E2E (jsdom)  |   1   |   1  |   0  |   0   |   0   |

Stack smoke: both stacks (`react`, `fastapi`) from `docs/config/stack-smoke.md` started for real
(not mocked) and PASSed, with `JWT_SECRET_KEY` exported before backend boot (AUTH-08's fail-fast
startup requirement).

### Stack smoke detail

| Stack   | Command                                                    | Healthcheck                                                      | Result |
|---------|-------------------------------------------------------------|---------------------------------------------------------------------|--------|
| fastapi | `JWT_SECRET_KEY=<random> uvicorn app.main:app --port 8000`   | `GET http://127.0.0.1:8000/api/health` → `200 {"status":"ok"}`        | PASS   |
| react   | `npm run dev -- --port 5173`                                 | `GET http://localhost:5173/` → `200` (127.0.0.1 connection-refused is a Vite default-bind artifact, not a boot failure — confirmed reachable on `localhost`) | PASS   |

### Live end-to-end login smoke (real servers, real DB, real bcrypt/JWT)

Requested opportunistic check: confirm the AUTH-03 `must_reset_password` fix and AUTH-05's real
UI wiring hold together end-to-end (server side; UI click-through itself remains
AUTH-08-TC-10/11 documented manual scope).

```
$ curl -X POST http://127.0.0.1:8000/api/users -d '{"name":"Smoke Test","email":"smoketest_<ts>@example.com","phone":"1234567890","role":"User","status":"Active","password":"SmokeTest123!"}'
201 → {"name":"Smoke Test","email":"smoketest_<ts>@example.com", ..., "id":6, ...}

$ curl -X POST http://127.0.0.1:8000/api/auth/login -d '{"email":"smoketest_<ts>@example.com","password":"SmokeTest123!"}'
HTTP_CODE:200
{"token":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...."}

$ curl -X POST http://127.0.0.1:8000/api/auth/login -d '{"email":"nonexistent@example.com","password":"wrongpass"}'
HTTP_CODE:401
{"detail":"Invalid email or password."}
```

Result: a freshly-created user (via `POST /api/users` with the new `password` field) can log in
immediately — `UserService.create_user()`'s `must_reset_password=False` fix (commit `eb946e72`)
is confirmed working end-to-end against the real backend, and the bad-credential path still
returns a generic `401` with no user-existence leakage. Both dev-server processes were killed
cleanly after the smoke check; no stale process remained on 8000/5173.

### Full independent test-suite re-run (not trusting self-reports)

```
$ cd frontend && npm run test
 Test Files  14 passed (14)
      Tests  93 passed (93)
   Duration  3.31s

$ cd backend && pytest -q
57 passed, 1 deselected, 5 warnings in 44.50s

$ cd frontend && npm run build
✓ 98 modules transformed, built in 627ms
```

93 frontend tests (up from 70 in round 2 — AUTH-06/AUTH-07/AUTH-08 test files have since landed
on the shared branch) include all 11 `AUTH-05-TC-*`-labeled tests in `App.authGate.test.jsx` plus
`App.auth.perf.test.jsx` (TC-09 performance), and all pass alongside the newly-merged
`App.auth.test.jsx`, `App.logoutFocus.test.jsx` (AUTH-07), `Settings.test.jsx` (AUTH-06),
`userApi.auth.test.js`, `userApi.logout.test.js`. No cross-story regression. 57 backend tests
pass (1 deselected is the pre-existing marked-slow perf test, unrelated convention). Build is
clean.

### Render-branch-unchanged confirmation

Direct source inspection of `frontend/src/App.jsx` confirms AUTH-05's owned logic is
byte-for-byte the same shape validated in round 2, despite AUTH-06/AUTH-07 growth around it:

- `if (!isAuthenticated) { return <LoginScreen ... /> }` branch present, unchanged.
- `handleLoginSuccess` still calls `setActiveView("dashboard")` unconditionally.
- Zero `authError` references in `App.jsx` (only a comment documenting the locked contract).
- AUTH-07's `logout()` export and AUTH-06's real Settings screen are consumed by other stories'
  code paths; neither required a change to AUTH-05's own render-branch condition or handler
  signature.

### Task completion verification (MANDATORY)

Cross-checked against `docs/features/AUTH-05/PLAN.md` § 5 Task Breakdown.

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|----------------|---------|
| T-01 | `frontend/src/App.jsx` (isAuthenticated-gated conditional render branch) | ✓ | ✓ — `if (!isAuthenticated) return <LoginScreen .../>` present; `handleLoginSuccess` calls `setActiveView("dashboard")`; zero `authError` references | PASS |
| T-02 | `frontend/src/pages/__tests__/App.routing.test.jsx` (getStoredToken/registerUnauthorizedHandler mock stubs) | ✓ | ✓ — `getStoredToken: vi.fn(() => MOCK_TOKEN)` and `registerUnauthorizedHandler: vi.fn()` both present | PASS |
| T-03 | `frontend/src/__tests__/App.authGate.test.jsx` (AUTH-05-TC-01..TC-11 coverage) | ✓ | ✓ — 26 `TC-0N`/`TC-1N` references found across the 11 labeled test blocks; all pass | PASS |

No PLAN task regressed. Task-completion verdict: PASS, no drop to PARTIAL.

### Passed

| TC id         | Title                                                                                     |
|---------------|---------------------------------------------------------------------------------------------|
| AUTH-05-TC-01 | Unauthenticated visitor sees Login-only view, no Sidebar/Header mounted                     |
| AUTH-05-TC-02 | activeView cannot be set to a protected view while unauthenticated                          |
| AUTH-05-TC-03 | Authenticated user sees Sidebar with exactly three working nav entries                      |
| AUTH-05-TC-04 | Successful login transitions directly to Dashboard without reload                           |
| AUTH-05-TC-05 | Post-login activeView resets to dashboard even if a prior session left it on users           |
| AUTH-05-TC-06 | Auth state becoming invalid falls back to Login-only, unmounting the stale protected view    |
| AUTH-05-TC-07 | sessionExpiredMessage is threaded through to LoginScreen unchanged on session expiry          |
| AUTH-05-TC-08 | No authError prop is ever passed to LoginScreen                                             |
| AUTH-05-TC-09 | Auth-gate branch adds no additional network round trip on login/logout toggle (performance)  |
| AUTH-05-TC-10 | Protected components do not mount and no protected-endpoint call fires when unauthenticated (security) |
| AUTH-05-TC-11 | Sidebar nav-item buttons remain Tab-reachable in order with visible focus when authenticated  |

### Flaky tests

None.

### Performance budget breaches

None. AUTH-05-TC-09's budget (`p95 mount-to-isAuthenticated-set latency < 100ms across 100
iterations`) passed: `budget_pass: true`.

### Regression coverage

No regression-tagged TCs exist for AUTH-05. No TC in this manifest carries a
`regression-AUTH-05-TC-NN` tag from a prior fix-loop pass.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None. All 11 TCs are `automatable: true`. (The actual browser UI click-through of the login form
remains documented manual scope under AUTH-08-TC-10/11, tracked in AUTH-08's own test-case
manifest — not an AUTH-05 gap.)

### Genuine defects found this round

None. This was a clean regression pass — no new failures surfaced from the AUTH-06/AUTH-07/AUTH-08
merges or from the `must_reset_password` bugfix landing on top.

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  18
- test_runner_runs:  4   (npm run test x1, pytest x1, npm run build x1, preflight smoke-imports x1)
- stack_smoke_runs:  2   (fastapi start+healthcheck, react start+healthcheck) + 3 explicit curl checks (POST /api/users create, POST /api/auth/login success, POST /api/auth/login negative)
- duration_seconds:  ~660
- timestamp:         2026-08-18T15:54:39Z
```
