# Validation report — USR-01

- Date: 2026-08-14T19:40:00Z
- Verdict: PASS
- Mode: full (round 2, post-fix re-validation)
- Total: 25   Passed: 25   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~40 minutes (19:00 preflight start → 19:35 last flow → 19:40 report write)

**Round 2 note:** this is the post-fix re-validation pass following
`docs/features/USR-01/VALIDATION-20260814-1900.md` (round 1), which reported 24/25 pass with
USR-01-TC-13 FAILing due to a missing committed regression test (not a code defect — the
underlying `handleFormSubmit` error-handling in `Users.jsx` was already correct; round 1's
throwaway ad-hoc spec had confirmed this). The fix pass since round 1 added a committed test
titled `"USR-01-TC-13: a failed Edit User submission keeps the modal open and shows the server
error inline"` to `frontend/src/pages/__tests__/Users.crud.test.jsx` and re-tagged
`docs/test-cases/USR-01.json`'s TC-13 entry with `regression-TC-13`. This report re-runs the
**full** 25-TC suite (not a TC-13-only spot check) to confirm both the fix and the absence of
any regressions elsewhere.

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   2   |   2  |   0  |   0   |   0   |
| Integration  |  16   |  16  |   0  |   0   |   0   |
| E2E          |   4   |   4  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |  ← structural budget (no k6/locust; verified via request-shape assertions per PLAN.md § 7)

Stack-smoke details: preflight (`npm install`, `pip install -r requirements.txt`,
`python -c "import app.main"`) all passed with exit 0. Both stacks were booted fresh for this
round:

- **fastapi**: `uvicorn app.main:app --port 8000`. First boot attempt hit a stale squatter
  process already bound to port 8000 (leftover from an earlier session, PID 68367 + its
  `--reload` multiprocessing worker PID 77777) — killed both per the port-free-check protocol,
  then rebooted cleanly (PID 4021). `GET http://127.0.0.1:8000/api/health` → `200`.
- **react**: `npm run dev -- --port 5173`. Same squatter pattern found — a pre-existing Vite dev
  server (PID 68715/68689) was already listening on 5173 from an earlier session and had
  silently absorbed the first health-check curl; killed and rebooted cleanly (PID 4380).
  `http://localhost:5173/` → `200`. `http://127.0.0.1:5173/` → connection-refused — same
  documented IPv4/IPv6 Vite-binding quirk noted in `docs/features/SRF-01/VALIDATION-20260814-1130.md`
  and round 1 of this report; not a functional defect.
- Both processes torn down cleanly at the end of the smoke window; ports 8000/5173 confirmed
  free afterward.

Verdict justification: **PASS** — all 25 TCs pass on this run (25/25, no retries needed), no
regression-tagged TC failed, all 8 PLAN tasks verify, stack smoke passed for both stacks, and
the performance TC's structural budget holds (per the same precedent as round 1 and SRF-01).

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/src/pages/Users.jsx` (new page, search/filter/pagination/CRUD extraction) | ✓ | ✓ (PAGE_SIZE=50, 300ms debounce, inline Inactive-derivation comment present) | PASS |
| T-02 | `frontend/src/pages/Dashboard.jsx` (trim to overview-only) | ✓ | ✓ (no search input, no UserTable, no CRUD state; stats cards + description only, confirmed via grep) | PASS |
| T-03 | `frontend/src/App.jsx` (real `"users"` branch, no remap) | ✓ | ✓ (`activeView === "users" ? <Users .../> :` branch present at line 53; `import Users from "./pages/Users.jsx"` at line 5) | PASS |
| T-04 | `frontend/src/pages/__tests__/Dashboard.search.test.jsx` (adapt in place) | ✓ | ✓ (file preserved, 2 tests, both pass) | PASS |
| T-05 | `frontend/src/pages/__tests__/Users.search.test.jsx` (relocate debounce/filter/pagination) | ✓ | ✓ (6 tests, all passing) | PASS |
| T-06 | `frontend/src/pages/__tests__/Users.crud.test.jsx` (CRUD/stats/loading/empty/error) | ✓ | ✓ — **now 13/13 claimed TCs present**: the previously-missing edit-failure test (`USR-01-TC-13`) is committed at line 226, tagged `regression-TC-13` in a comment at line 223, and passing (`updateUser.mockRejectedValue`, asserts inline error + modal stays open) | **PASS** (round-1 gap closed) |
| T-07 | `frontend/src/pages/__tests__/App.routing.test.jsx` (nav/active-state/header/a11y) | ✓ | ✓ (3 tests, all passing) | PASS |
| T-08 | `README.md` §4 + §11 (Users.jsx in Project Structure; CRUD/search/filter/pagination moved to Users page) | ✓ | ✓ (`§4` lists `└── Users.jsx` at line 75; `§11` states "CRUD, search, filtering, and pagination all live on the dedicated **Users** page" at line 171) | PASS |

8/8 PLAN tasks fully verified. T-06's round-1 claim-mismatch (Notes column claimed TC-13
coverage that wasn't on disk) is resolved — the file now contains the committed test matching
the claim.

### Passed

| TC id         | Title                                                                 |
|---------------|------------------------------------------------------------------------|
| USR-01-TC-01  | Clicking Users nav renders the dedicated Users page with active-state highlighting |
| USR-01-TC-02  | Users page header shows the "+ Add User" button                       |
| USR-01-TC-03  | Four stats cards render with Total/Active/Inactive/Admin values       |
| USR-01-TC-04  | Inactive stat derivation verified against a known dataset             |
| USR-01-TC-05  | UserTable renders with columns ID/Name/Email/Phone/Role/Status/Created/Actions |
| USR-01-TC-06  | Non-empty search debounces 300ms before firing a request              |
| USR-01-TC-07  | Clearing search input triggers immediate (0ms) refetch                |
| USR-01-TC-08  | Changing Role or Status filter refetches immediately, resets to page 1 |
| USR-01-TC-09  | Clicking a row's view icon opens full detail via UserDetails          |
| USR-01-TC-10  | Adding a user with valid data succeeds end-to-end                     |
| USR-01-TC-11  | Adding a user with duplicate email surfaces 409 inline                |
| USR-01-TC-12  | Editing a user with valid changes succeeds end-to-end                 |
| USR-01-TC-13  | Editing a user that fails shows a readable inline error, never a raw stack trace (regression-TC-13, now committed and passing) |
| USR-01-TC-14  | Confirming delete calls DELETE, shows toast, refreshes list/stats     |
| USR-01-TC-15  | Canceling delete makes no request, row unchanged                      |
| USR-01-TC-16  | Pagination Previous/Next navigates pages, disables at boundaries      |
| USR-01-TC-17  | Loading states render for stats and table on initial load             |
| USR-01-TC-18  | Empty state shown when zero users exist, no filters active            |
| USR-01-TC-19  | No-results state shown when zero users match active search/filter     |
| USR-01-TC-20  | User-list fetch failure shows inline banner; stats failure shows toast |
| USR-01-TC-21  | Dashboard.search.test.jsx preserved (not deleted) after extraction    |
| USR-01-TC-22  | Users-page test suite reaches parity with original Dashboard coverage |
| USR-01-TC-23  | Users page requests stay paginated, search stays rate-bounded (300ms) |
| USR-01-TC-24  | Users page components route all HTTP calls through userApi.js         |
| USR-01-TC-25  | Users page toolbar controls expose accessible labels                  |

### Flaky tests

None.

### Performance budget breaches

None. USR-01-TC-23's NFR (`no more than 1 getUsers call per 300ms; pageSize=50 always present`)
has no numeric latency budget requiring a load-test runner — verified structurally via the same
fake-timer assertions in `Users.search.test.jsx` (TC-06/TC-07/TC-16 exact `page`/`pageSize`/
debounce-timing params). `budget_pass: true`, consistent with round 1 and the SRF-01 precedent.

### Regression coverage

| Regression TC | Guards against | Last attempt | Verdict |
|----------------|-----------------|--------------|---------|
| USR-01-TC-13 (`tags: ["negative", "regression-TC-13"]`) | Round-1 gap: PLAN.md T-06 claimed edit-failure inline-error coverage that was absent from `Users.crud.test.jsx`; risk of the edit-failure path silently regressing with no test to catch it | PASS (`updateUser.mockRejectedValue`, asserts inline error text + modal stays open) | **PASS** |
| USR-01-TC-21 (`tags: ["regression"]`) | `Dashboard.search.test.jsx` being deleted or left asserting stale (pre-extraction) behavior | PASS (2/2 tests) | PASS |
| USR-01-TC-22 (`tags: ["regression"]`) | Silent coverage loss during the Dashboard → Users test relocation | PASS (all original debounce/clear/aria-label cases relocated, plus new filter/pagination coverage) | PASS |
| USR-01-TC-24 (`tags: ["regression"]`) | A new direct `axios` import creeping into the extracted page/components | PASS (`grep -rn "import axios" frontend/src/pages frontend/src/App.jsx` → no matches) | PASS |

No regression-tagged TC failed. TC-13's re-run confirms the fix is clean — the bug identified in
round 1 (missing committed test, not a runtime defect) has not re-surfaced, and no other
regression-tagged TC was disturbed by the fix.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — all 25 TCs are `automatable: true`.

### Budget-unparseable NFRs

None.

### Additional environment/scale notes (not failures)

- **Real backend CRUD cycle** was exercised directly against `http://127.0.0.1:8000/api` (fresh
  boot, this round):
  - Baseline stats: `{"total_users":5,"active_users":4,"admin_users":2,"regular_users":3}`.
  - `POST /api/users` (valid, `round2.validation@test.com`) → `id: 6`; stats recomputed to
    `6/5/2/4`.
  - `GET /api/users?search=Round2` → returned exactly the newly created user (`total: 1`).
  - `POST /api/users` with the same email again → `409` (duplicate-email conflict enforced
    server-side).
  - `GET /api/users?role=Admin` → 2 admins (David Lee, Emily Davis); `GET
    /api/users?status=Inactive` → 1 inactive user (Michael Brown) — filters verified against
    real seeded data.
  - `PUT /api/users/6` (status Active→Inactive) → `200`; stats recomputed to `6/4/2/4`
    (active_users correctly dropped by 1).
  - `DELETE /api/users/6` → `204`; subsequent `GET /api/users/6` → `404`; stats reverted to the
    exact baseline `5/4/2/3` — Inactive derivation (`total - active`) stayed internally
    consistent through the full create → search → filter → update → delete cycle.
  - Pagination at real scale: backend seed data is still 5 users (`PAGE_SIZE = 50`), so
    Previous/Next boundary behavior is exercised via `Users.search.test.jsx`'s mocked
    `total: 120` 3-page walk, same as round 1 — not re-testable against the live seeded backend
    at this data volume, noted as a pre-existing scale limitation, not a new gap.

### Proof of run

```
### Proof of run

- session_id:        97533432-5120-43ce-9e95-c8f6508c3ccf
- bash_invocations:  24
- test_runner_runs:  3 (pytest -q [backend, 16 tests]; npm run test [frontend/vitest, 5 files / 25 tests]; npx vitest run -t "TC-13" [targeted re-confirmation, 2 tests])
- stack_smoke_runs:  2 (fastapi: uvicorn on :8000, killed 1 stale squatter pair + clean reboot + GET /api/health poll; react: vite on :5173, killed 1 stale squatter pair + clean reboot + GET localhost:5173 poll)
- duration_seconds:  ~2400
- timestamp:         2026-08-14T19:40:00Z
```
