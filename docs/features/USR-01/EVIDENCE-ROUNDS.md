# Evidence rounds — USR-01

| Round | FAILing dims | Action | Result |
|-------|--------------|--------|--------|
| 1 | (validation, not evidence-pass) USR-01-TC-13 missing regression test | validation-execution found `frontend/src/pages/__tests__/Users.crud.test.jsx` had no committed test for the edit-failure inline-error path (create-failure path was covered, edit-failure was not). Root cause: T-06 treated the happy-path edit test as sufficient coverage for "edit" generally, leaving the shared `handleFormSubmit` catch block's edit branch untested. Code (`frontend/src/pages/Users.jsx:135-153`) already behaved correctly — this was a missing-test gap, not a code defect. Fix: added test `USR-01-TC-13: a failed Edit User submission keeps the modal open and shows the server error inline` to `Users.crud.test.jsx`, mocking `userApi.updateUser` rejection and asserting the inline error text plus that the `Edit User` modal stays open. Updated `docs/test-cases/USR-01.json` TC-13 entry (`tags` +`regression-TC-13`, `then` clause references the new test, `last_run.status` PASS, new attempt row) without duplicating the id. | `unit_tests` (frontend, supporting) 25/25 ✓ — new test passes, no regressions |
| 2 | none | Re-ran full evidence packet (backend pytest 16/16, frontend vitest 25/25, build, runtime unchanged from prior session) to confirm no regressions from the test-file edit. | All PASS / accepted-N/A |
