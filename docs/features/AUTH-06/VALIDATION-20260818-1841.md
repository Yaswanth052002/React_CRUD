# Validation report — AUTH-06

- Date: 2026-08-18T18:41:00Z
- Verdict: **PASS**
- Mode: full (round 2 — re-validation after the round-1 `GET /api/auth/me` missing-endpoint fix)
- Total: 13   Passed: 13   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~35 minutes (preflight + stack smoke + live contract check + full suites + build)

This is round 2 of `/arh-validate-feature` for AUTH-06, run after the fix for the critical
regression found in round 1 (`docs/features/AUTH-06/VALIDATION-20260818-1601.md`): a live
end-to-end check found `GET /api/auth/me` did not exist anywhere in the merged backend. The fix
(`backend/app/services/auth_service.py::get_current_user_from_token`, `backend/app/schemas/auth.py::CurrentUserResponse`,
`backend/app/api/auth.py::GET /api/auth/me`, plus 3 new tests in `backend/tests/test_auth.py`) is
uncommitted in the working tree at HEAD of `feature/AUTH`. This round re-runs the exact live
contract check from round 1 against the fixed code, and re-runs Phase 1–3 in full rather than
trusting the fix author's own claims.

## Summary

| Layer                                              | Total | Pass | Fail | Error | Flaky |
|-----------------------------------------------------|-------|------|------|-------|-------|
| Stack smoke                                          |   2   |   2  |   0  |   0   |   0   |
| AUTH-06 TCs (vitest, `Settings.test.jsx`)             |  12   |  12  |   0  |   0   |   0   |
| Regression TC (live contract, backend)                |   1   |   1  |   0  |   0   |   0   |
| Frontend full suite (`npm run test`)                  |  93   |  93  |   0  |   0   |   0   |
| Backend full suite (`pytest`)                         |  61   |  60  |   0  |   0   |   1   |
| Frontend build (`npm run build`)                      |   1   |   1  |   0  |   0   |   0   |
| **Live contract check** (create → login → `GET /api/auth/me`, real running server) | 3 | 3 | 0 | 0 | 0 |

Stack smoke: both `fastapi` (uvicorn, port 8000, `JWT_SECRET_KEY` set to an ephemeral local test
value — matches `.env.example`'s documented required-var contract, no real secret used) and
`react` (Vite dev server, port 5173) started cleanly and answered their configured checks —
`fastapi`: `GET http://127.0.0.1:8000/api/health` → `200`; `react`: `GET http://localhost:5173/` →
`200` (same documented IPv4/`127.0.0.1` vs `localhost` binding note as round 1 — Vite binds to
`localhost`, not `127.0.0.1`; not a regression).

**Live contract check (the exact round-1 reproduction, re-run against the fixed code):**
1. `POST /api/users` (fresh user, id 6) → `200`.
2. `POST /api/auth/login` with that user's credentials → `200`, valid 3-part JWT (`sub`=email).
3. `GET /api/auth/me` with `Authorization: Bearer <token>` → **`200 {"name": "Round Two Smoke", "email": "round2.smoke.<ts>@example.com"}`** — exactly `{name, email}`, no extra fields. This is the fix confirmation for LIVE-01.
4. `GET /api/auth/me` with no `Authorization` header → `401 {"detail": "Could not validate credentials."}`.
5. `GET /api/auth/me` with `Authorization: Bearer not-a-real-token` → `401 {"detail": "Could not validate credentials."}` — identical generic message to (4), confirming the `HTTPBearer(auto_error=False)` normalization fix from the code-review medium finding also works correctly end-to-end.

**Settings.jsx response-shape confirmation:** `frontend/src/services/userApi.js::getCurrentUser()`
calls `client.get("/api/auth/me")` and returns `res.data` unmodified (`userApi.js:201-208`);
`Settings.jsx` reads only `currentUser.name` / `currentUser.email` off that result
(`Settings.jsx` render tree per PLAN.md § 3). The real endpoint's response body confirmed above
(`{"name": ..., "email": ...}`) is byte-for-byte the shape `Settings.jsx` consumes — no full
browser click-through was needed to prove this because `getCurrentUser()` passes the response
through with zero transformation; the live curl response IS what `Settings.jsx` receives.

**Backend full suite note:** 1 test (`tests/test_password_hashing.py::test_hash_password_p95_latency_within_budget`,
an AUTH-03-owned bcrypt-latency benchmark, unrelated to AUTH-06) failed on the first run
(336ms vs a 300ms p95 budget) and passed cleanly in isolation on immediate retry (per the
retry-on-flake procedure) — flagged as **Flaky**, machine-load-dependent, not an AUTH-06
regression, and does not affect this verdict (retry attempt: PASS).

## Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|----------------|---------|
| T-01 | `frontend/src/services/userApi.js` (add `getCurrentUser()` export) | ✓ | ✓ (`export async function getCurrentUser()` at line 201, `client.get("/api/auth/me")`, `normalizeError` on catch, mirrors `getUser`/`getDashboardStats`) | PASS |
| T-02 | `frontend/src/pages/Settings.jsx` (build Settings page component) | ✓ | ✓ (props `{onMenuClick, onLogout}`, no `isAuthenticated`; `useState` for `currentUser`/`loadingUser`/`error`; empty-dep `useEffect`; `handleLogout` calls `userApi.logout()` then `onLogout()`; spinner + inline error banner + details-item labelling all present) | PASS |
| T-03 | `frontend/src/App.jsx` (swap `SettingsPlaceholder` for `Settings`) | ✓ | ✓ (line 104: `<Settings onMenuClick={() => setSidebarOpen(true)} onLogout={handleLogout} />` inside the `activeView === "settings"` branch) | PASS |
| T-04 | `frontend/src/pages/__tests__/Settings.test.jsx` (unit tests, AUTH-06-TC-01..TC-12) | ✓ | ✓ (12/12 tests present and passing, one per TC) | PASS |
| T-05 | `README.md` (§ 11 CRUD Usage — Settings read-only note) | ✓ | ✓ (line 255 "Logout" item + line 258 confirming Settings is read-only, shows signed-in user's own name/email, no editing) | PASS |

All 5/5 PLAN tasks verified intact. No drift found in `Settings.jsx`'s contract versus PLAN.md § 3.
(The round-1 gap — `GET /api/auth/me` — was never a PLAN.md task for any of AUTH-02/AUTH-04/AUTH-06;
it is fixed directly in `backend/app/api/auth.py`/`services/auth_service.py`/`schemas/auth.py`,
outside this PLAN's file table, per the fix description; the fix does not conflict with or
duplicate any PLAN task above.)

### Passed

| TC id          | Title                                                                 |
|----------------|------------------------------------------------------------------------|
| AUTH-06-TC-01  | getCurrentUser() returns name and email on success                    |
| AUTH-06-TC-02  | getCurrentUser() throws a normalizeError-wrapped error on failure     |
| AUTH-06-TC-03  | getCurrentUser() response never includes role, id, or avatar fields   |
| AUTH-06-TC-04  | Logout button calls userApi.logout() then props.onLogout() in order   |
| AUTH-06-TC-05  | Settings.jsx performs no navigation or redirect itself on logout      |
| AUTH-06-TC-06  | Settings.jsx does not accept or branch on an isAuthenticated prop     |
| AUTH-06-TC-07  | Loading spinner renders next to fields while getCurrentUser() is pending |
| AUTH-06-TC-08  | Inline persistent error banner renders on fetch failure               |
| AUTH-06-TC-09  | Current-user fetch is issued exactly once on mount with no retry or polling |
| AUTH-06-TC-10  | Current-user identity is derived server-side, never from a client-supplied id |
| AUTH-06-TC-11  | No PII is written to logs or error messages on fetch success or failure |
| AUTH-06-TC-12  | Name/email fields use the details-item labelling pattern               |
| AUTH-06-TC-13  | (regression) GET /api/auth/me exists on the real backend and returns exactly {name, email} | 

All 13 TCs pass. TC-01..TC-12 re-ran clean at the mocked-unit layer (`Settings.test.jsx`, 12/12,
182ms) — unchanged from round 1. TC-13 is the new regression-tagged TC added this round (see
below) and is the one that actually exercises the real, previously-missing endpoint.

## Flaky tests

### `test_hash_password_p95_latency_within_budget` (backend, AUTH-03-owned, not an AUTH-06 TC)

- Attempt history: FAIL (336ms vs 300ms budget) → PASS (retry, isolated run)
- Final verdict: FLAKY (machine-load-dependent bcrypt timing on this run's hardware)
- Not in `docs/test-cases/AUTH-06.json` scope; does not affect AUTH-06's verdict. Recorded here
  for visibility since the full `pytest` suite was run per this round's instructions.

## Regression coverage

| Regression TC      | Guards against | Last attempt | Verdict |
|---------------------|-----------------|--------------|---------|
| AUTH-06-TC-13 (new, tagged `regression-AUTH-06-TC-01`) | Round-1 LIVE-01 finding: `GET /api/auth/me` did not exist on the real backend, so `Settings.jsx`/`getCurrentUser()` always failed in production despite all mocked TCs passing | PASS (live curl: 200 exact `{name, email}`; 401 for no-token and invalid-token, identical generic message) | **PASS** |

`AUTH-06-TC-13` was added this round per the regression-tag protocol, pointing at the three new
`backend/tests/test_auth.py` cases (`test_me_returns_exactly_name_and_email_for_the_bearer_tokens_owner`,
`test_me_without_a_token_is_rejected`, `test_me_with_an_invalid_token_returns_generic_401`) and
independently re-verified live against a freshly started, real uvicorn server (not just via
`pytest`) — the exact reproduction steps from round 1's LIVE-01 finding. `coverage_audit` was
re-run after the append; `uncovered: []` still holds (`AUTH-06-TC-13` added to `AUTH-06-FR-1`'s
`covered_by` list).

**The regression is genuinely closed**, confirmed at three independent layers:
1. `pytest backend/tests/test_auth.py -k me` — 3/3 new tests pass.
2. Live curl against a real, freshly started uvicorn process (not the test client) — 200 with the
   exact expected shape, 401 for both negative cases.
3. Code-path trace confirms `Settings.jsx` → `userApi.getCurrentUser()` → `client.get("/api/auth/me")`
   receives that exact real response unmodified.

## Failed (bug blocks)

None.

## Manual follow-up

None. All 13 AUTH-06-scoped TCs (12 original + 1 regression) are `automatable: true`.

## Budget-unparseable NFRs

None — AUTH-06 carries no `type: performance` TC with a numeric budget (TC-09's assertion is a
call-count check, not a latency benchmark, per PLAN.md § 7).

### Proof of run

```
### Proof of run

- session_id:        64b4955b-bc8f-4895-9272-a2d38528e380
- bash_invocations:  21
- test_runner_runs:  7  (npm install; npx vitest run Settings.test.jsx; npm run test; pytest -q; pytest -q test_password_hashing retry; pytest -q -k auth; npm run build)
- stack_smoke_runs:  2  (fastapi uvicorn start+healthcheck; vite dev-server start+load-check)
- duration_seconds:  ~2100
- timestamp:         2026-08-18T18:41:17Z
```
</content>
