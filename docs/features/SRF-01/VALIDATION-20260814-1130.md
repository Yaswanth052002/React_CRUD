# Validation report — SRF-01

- Date: 2026-08-14T11:30:00Z
- Verdict: PARTIAL
- Mode: full
- Total: 9   Passed: 8   Failed: 0   Errored: 1   Flaky: 0   Skipped manual: 0
- Duration: ~14 minutes (11:07 UTC preflight start → 11:20 UTC last flow → 11:30 UTC report write)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   3   |   3  |   0  |   0   |   0   |
| Integration  |   4   |   4  |   0  |   0   |   0   |
| E2E          |   0   |   0  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   0  |   0  |   1   |   0   |  ← not executable, no load-test runner exists (deferred, not a defect)

Note: TC-09 (`type: e2e`) is executed as a static jsdom DOM-attribute assertion inside the Vitest unit run per PLAN.md T-01 rationale — it is counted under "Unit" above alongside TC-01/TC-02, matching the 3 tests reported by `Dashboard.search.test.jsx`.

Stack-smoke: both stacks (`react`, `fastapi`) started cleanly and answered their healthchecks. `react`'s Vite dev server bound only to IPv6 `localhost` (curl to `127.0.0.1:5183` connection-refused; curl to `http://localhost:5183/` returned 200) — a known IPv4/IPv6 mismatch, not a functional defect; recorded here per the escalation guidance in `validation-execution` § Preflight.

Verdict justification: PARTIAL — SRF-01-TC-07 (performance) could not be executed (`budget_pass: null`, no load-test runner in repo); this is a pre-accepted scope-out per REQUIREMENTS.md § Scope (Out) and `pending_carry_forward` in `state.json`, not a functional regression, but per the `validation-execution` verdict rule any performance TC without `budget_pass: true` caps the report at PARTIAL.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `frontend/src/pages/__tests__/Dashboard.search.test.jsx` (new Vitest spec, TC-01/TC-02/TC-09) | ✓ | ✓ (3/3 tests present and passing: debounce, immediate-clear, aria-label) | PASS |
| T-02 | `backend/tests/test_users.py` (re-verify only, no edits) | ✓ | ✓ (full suite re-run: 16/16 passed, including `test_search_filters_by_name_email_phone`) | PASS |

Both PLAN.md `## 5. Task Breakdown` rows verified complete. `docs/features/SRF-01/state.json` `.impl_tasks` independently confirms both T-01 and T-02 marked `"status": "done"`.

### Passed

| TC id         | Title                                                              |
|---------------|---------------------------------------------------------------------|
| SRF-01-TC-01  | Non-empty search value debounces 300ms before firing request        |
| SRF-01-TC-02  | Clearing the search input triggers an immediate (0ms) refetch       |
| SRF-01-TC-03  | Search matches case-insensitively across name, email, and phone     |
| SRF-01-TC-04  | Empty search term returns full unfiltered, paginated user list      |
| SRF-01-TC-05  | Search term matching no user returns empty result set, not an error |
| SRF-01-TC-06  | Backend regression suite passes after test-infrastructure fix       |
| SRF-01-TC-08  | Search endpoint requires no authentication (documented behavior)    |
| SRF-01-TC-09  | Search input exposes an accessible label                            |

### Flaky tests

None.

### Performance budget breaches

### SRF-01-TC-07 — GET /api/users search response time stays within performance budget (NFR-performance)

- Target:   p95 < 300ms @ 50 RPS
- Measured: null — not executed
- budget_pass: null
- Action: no load-testing runner (k6/locust/equivalent) exists in this repo. Explicitly out of scope per REQUIREMENTS.md § Scope (Out): "Measuring/tuning the server-side p95 latency budget under real load — tracked as a follow-up profiling task, not blocking this PRD." Already recorded as `pending_carry_forward` item `SRF-01-perf-p95-measurement` in `docs/features/SRF-01/state.json`. Recommend adding k6/locust to `docs/config/project-commands.yaml` in a follow-up story before this NFR can be enforced.

### Regression coverage

| Regression TC | Guards against | Last attempt | Verdict |
|----------------|-----------------|--------------|---------|
| SRF-01-TC-06 (`tags: ["regression-test-infra-fix"]`) | Prior `test_users.py:13` import-shadowing bug (research Risk #1) | PASS (16/16, 0.61s) | PASS |
| SRF-01-TC-08 (`tags: ["regression"]`) | Accidental introduction of auth on the search endpoint | PASS (200 OK, no Authorization header) | PASS |
| SRF-01-TC-03 (`tags: ["regression"]`) | Case-sensitivity / cross-field search regression | PASS (ALICE matched Alice Example only, Bob excluded) | PASS |

No regression-tagged TC failed.

### Failed (bug blocks)

None.

### Errored

### SRF-01-TC-07 — GET /api/users search response time stays within performance budget

- Reason: no k6/locust/load-test runner configured in `docs/config/project-commands.yaml` or elsewhere in the repo
- Likely cause: intentional scope exclusion (see REQUIREMENTS.md § Scope (Out) and PLAN.md § Test Strategy) — not a code defect
- Action: SKIPPED/DEFERRED, not FAIL. Already tracked in `state.json.pending_carry_forward` (`SRF-01-perf-p95-measurement`, owner: "Full-stack (brownfield backfill — no named owner in source system)"). No further action needed from this validation run beyond re-confirming the carry-forward entry is still present (it is).

### Manual follow-up

None — all 9 TCs are `automatable: true`.

### Budget-unparseable NFRs

None — the NFR-performance budget (`p95 < 300ms @ 50 RPS`) is fully parseable; it simply has no runner to execute it against (`budget_pass: null`, not `unparseable`).

---

## Per-TC one-line results

| TC id | Result | Notes |
|---|---|---|
| SRF-01-TC-01 | PASS | Vitest, `Dashboard.search.test.jsx`, fake-timer 300ms debounce assertion |
| SRF-01-TC-02 | PASS | Vitest, same file, 0ms immediate-clear refetch assertion |
| SRF-01-TC-03 | PASS | Real backend `GET /api/users?search=ALICE` → only Alice Example returned (Bob excluded), case-insensitive match confirmed against live-created fixture users |
| SRF-01-TC-04 | PASS | Real backend `GET /api/users` (no search param) → full envelope `{items, total, page, page_size}` returned |
| SRF-01-TC-05 | PASS | Real backend `GET /api/users?search=zzz-nomatch-xyz` → 200 OK, `items: [], total: 0` |
| SRF-01-TC-06 | PASS | `pytest` in `backend/` → 16/16 passed (2 unrelated `on_event` deprecation warnings only) |
| SRF-01-TC-07 | ERROR/DEFERRED | No load-test runner in repo; pre-accepted scope-out, tracked in `state.json.pending_carry_forward` |
| SRF-01-TC-08 | PASS | Real backend `GET /api/users?search=alice` with no Authorization header → 200 OK |
| SRF-01-TC-09 | PASS | Vitest, same file, static aria-label="Search users" assertion |

### Proof of run

```
### Proof of run

- session_id:        67dd287c-6d7c-4634-9598-cfd4eebdc50c
- bash_invocations:  17
- test_runner_runs:  2 (npm run test -- Dashboard.search [vitest, 3 tests]; pytest -q [16 tests])
- stack_smoke_runs:  2 (fastapi: uvicorn on :8011 + GET /api/health poll; react: vite on :5183 + GET / poll)
- duration_seconds:  ~840
- timestamp:         2026-08-14T11:30:00Z
```
