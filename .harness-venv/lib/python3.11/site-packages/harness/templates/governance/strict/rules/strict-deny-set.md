# Strict — extended deny set

<!-- Harness scaffold: governance=strict rule=strict-deny-set -->
<!-- Extends the `standard` profile deny set with stricter prohibitions. -->
<!-- Loads unconditionally when governance.profile=strict. -->
<!-- Examples of intent (delete and replace with team-specific rules): -->
<!--   - No `Bash(curl|wget) http://` (only https) -->
<!--   - No reads of `**/private/**` outside `docs/` -->
<!--   - No writes to `.github/workflows/*` outside the CI agent -->
<!--   - No `git commit --no-verify` even when explicitly approved -->

## Rules

<!-- TODO: fill with team-specific stricter deny list -->

## Examples

<!-- TODO: BAD vs GOOD examples for each rule -->
