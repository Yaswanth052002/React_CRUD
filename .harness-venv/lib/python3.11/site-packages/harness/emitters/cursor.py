"""Cursor emitter — v2 stub."""

from __future__ import annotations

from harness.emitters.base import EmitOptions, EmitResult
from harness.ir.model import HarnessIR


class CursorEmitter:
    target = "cursor"

    def supported_features(self) -> set[str]:
        return {"rules", "memory_via_cursorrules"}

    def emit(self, ir: HarnessIR, opts: EmitOptions) -> EmitResult:
        raise NotImplementedError(
            "Cursor emitter is deferred to v2. Target is .cursor/rules/*.mdc with paths→globs."
        )
