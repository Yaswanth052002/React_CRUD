"""Codex CLI emitter — v2 stub. See plan §3."""

from __future__ import annotations

from harness.emitters.base import EmitOptions, EmitResult
from harness.ir.model import HarnessIR


class CodexEmitter:
    target = "codex"

    def supported_features(self) -> set[str]:
        return {"memory", "skills_collapsed_into_agents_md"}

    def emit(self, ir: HarnessIR, opts: EmitOptions) -> EmitResult:
        raise NotImplementedError(
            "Codex CLI emitter is deferred to v2. See plan §3 — "
            "AGENTS.md target with collapsed skills/agents and config.toml MCP block."
        )
