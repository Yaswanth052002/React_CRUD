"""GitHub Copilot emitter — v2 stub."""

from __future__ import annotations

from harness.emitters.base import EmitOptions, EmitResult
from harness.ir.model import HarnessIR


class CopilotEmitter:
    target = "copilot"

    def supported_features(self) -> set[str]:
        return {"memory_only"}

    def emit(self, ir: HarnessIR, opts: EmitOptions) -> EmitResult:
        raise NotImplementedError(
            "Copilot emitter is deferred to v2. Target is "
            ".github/copilot-instructions.md with all skills/agents/rules collapsed."
        )
