# Activity feed

{% if not by_user %}
No activity recorded yet. Run any Harness command to start populating
`docs/activity/<YYYY-MM>.jsonl` via the Stop / SessionEnd hook.
{% else %}
Window: **{{ window_label }}** &nbsp;·&nbsp; Source: `docs/activity/*.jsonl`

{% for user, rows in by_user %}
## 👤 {{ user }}  ({{ rows|length }} cmds · {{ user_totals[user] }})

| Timestamp | Command | Feature | Status | Int | Files +/~ | Lines+ | Rej |
|-----------|---------|---------|--------|-----|-----------|--------|-----|
{% for e in rows -%}
| {{ e.ts.strftime('%Y-%m-%d %H:%M') }} | `{{ e.command or '—' }}` | {% if e.feature and e.feature in feature_ids %}[{{ e.feature }}](features/{{ e.feature }}/index.md){% elif e.feature %}{{ e.feature }}{% else %}—{% endif %} | {{ outcome_icon(e.outcome) }} | {{ intervention_marker(e.intervention_count) }} | {{ e.files_created }} / {{ e.files_modified }} | {{ e.lines_added }} | {{ e.tool_rejections }} |
{% endfor %}
{% endfor %}
{% endif %}
