# Features

{{ feature_count }} feature{% if feature_count != 1 %}s{% endif %} across this project.

<div class="grid cards" markdown>

{% for fs in features %}
-   **[{{ fs.feature_id }}]({{ fs.feature_id }}/index.md)** &nbsp; {{ status_signal(fs)[0] }}

    {{ fs.title }}

    _{{ status_signal(fs)[1] }}_

{% endfor %}
</div>
