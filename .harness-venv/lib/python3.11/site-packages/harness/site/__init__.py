"""MkDocs site generator for Harness artifacts.

Reads a downstream project's `docs/`, `.claude/`, and `CLAUDE.md` and emits a
browseable MkDocs site. The orchestrator is `harness.site.builder.build_site()`;
the CLI entry is `harness.commands.site`.
"""
